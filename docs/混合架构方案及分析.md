# 网络监测系统优化方案

## 文档信息

| 项目 | 内容         |
| -- | ---------- |
| 版本 | v1.5       |
| 日期 | 2026-03-26 |
| 状态 | 阶段五已完成（全部完成）        |

***

## 一、系统定位与架构概述

### 1.1 系统定位

本项目定位为**轻量级网络监测系统**，具有以下特点：

- **单网关部署**：与单一网关联动，无需集群设计
- **轻量化技术栈**：避免引入复杂组件，降低运维成本
- **核心功能聚焦**：专注于攻击检测、防御联动、告警通知

### 1.2 架构设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        轻量级混合架构                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   第一层防御（网关本地，实时）                                                │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 限流检测 ──> 本地计数，毫秒级响应                                        │
│   • 黑名单拦截 ──> 本地缓存，直接拦截                                        │
│   • 状态机 ──> NORMAL/SUSPICIOUS/ATTACKING/DEFENDED 本地判断                │
│   • 事件推送 ──> 推送攻击事件到监测服务                                      │
│                                                                             │
│   第二层防御（监测服务，异步）                                                │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 攻击事件聚合 ──> 合并重复事件                                            │
│   • 攻击记录存储 ──> 持久化攻击数据                                          │
│   • 态势感知 ──> 攻击态势展示                                                │
│   • 告警通知 ──> 邮件/飞书通知                                               │
│                                                                             │
│   数据流向:                                                                  │
│   网关 ──(事件)──> 监测服务 ──(指令)──> 网关                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

***

## 二、当前实现架构分析

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        当前实现架构                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                              请求流量                                        │
│                                 │                                           │
│                                 ▼                                           │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         网关服务 (9000)                              │   │
│   │  ┌─────────────────────────────────────────────────────────────┐   │   │
│   │  │ TrafficCollectGlobalFilter                                   │   │   │
│   │  │ - 采集流量数据                                                │   │   │
│   │  │ - 根据状态决定推送策略(实时/采样/批量/跳过)                     │   │   │
│   │  └─────────────────────────────────────────────────────────────┘   │   │
│   │                                 │                                   │   │
│   │                                 ▼                                   │   │
│   │  ┌─────────────────────────────────────────────────────────────┐   │   │
│   │  │ RequestRateLimitFilter                                       │   │   │
│   │  │ - 限流计数                                                    │   │   │
│   │  │ - 状态机自动转换 (NORMAL→SUSPICIOUS→ATTACKING→DEFENDED)      │   │   │
│   │  │ - 推送DDoS事件到监测服务                                       │   │   │
│   │  │ - 推送黑名单事件到监测服务                                      │   │   │
│   │  └─────────────────────────────────────────────────────────────┘   │   │
│   │                                 │                                   │   │
│   │                                 ▼                                   │   │
│   │  ┌─────────────────────────────────────────────────────────────┐   │   │
│   │  │ IpBlacklistDefenseFilter                                     │   │   │
│   │  │ - 检查黑名单缓存                                              │   │   │
│   │  │ - 拦截已封禁IP (返回403)                                      │   │   │
│   │  └─────────────────────────────────────────────────────────────┘   │   │
│   │                                                                     │   │
│   │  核心缓存:                                                          │   │
│   │  - IpAttackStateCache (状态机)                                     │   │
│   │  - IpBlacklistCache (黑名单)                                       │   │
│   │  - RequestRateLimitCache (限流计数)                                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                 │                                           │
│                    ┌────────────┼────────────┐                              │
│                    │ 推送事件   │ 推送流量   │ 接收指令                      │
│                    ▼            ▼            ▲                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         监测服务 (9002)                              │   │
│   │                                                                     │   │
│   │  接收接口:                                                          │   │
│   │  - /api/inner/traffic/receive (流量数据)                            │   │
│   │  - /api/inner/attack/ddos-event (DDoS事件)                          │   │
│   │  - /api/inner/blacklist/event (黑名单事件)                          │   │
│   │  - /api/inner/defense/log/sync (防御日志)                           │   │
│   │                                                                     │   │
│   │  处理逻辑:                                                          │   │
│   │  ┌─────────────────────────────────────────────────────────────┐   │   │
│   │  │ DDoSDetectServiceImpl                                        │   │   │
│   │  │ - 独立的DDoS检测逻辑 (与网关重复!)                            │   │   │
│   │  │ - 独立的请求计数器                                            │   │   │
│   │  └─────────────────────────────────────────────────────────────┘   │   │
│   │                                 │                                   │   │
│   │                                 ▼                                   │   │
│   │  ┌─────────────────────────────────────────────────────────────┐   │   │
│   │  │ DefenseDecisionServiceImpl                                   │   │   │
│   │  │ - 接收攻击记录后生成防御指令                                   │   │   │
│   │  │ - 推送防御指令到网关                                          │   │   │
│   │  │ - 记录防御日志                                                │   │   │
│   │  │ - 发布黑名单同步事件                                          │   │   │
│   │  └─────────────────────────────────────────────────────────────┘   │   │
│   │                                                                     │   │
│   │  发送接口:                                                          │   │
│   │  - /api/gateway/defense/command (防御指令)                         │   │
│   │  - /api/gateway/defense/state/sync (状态同步)                      │   │
│   │                                                                     │   │
│   │  核心缓存:                                                          │   │
│   │  - IpAttackStateCache (状态缓存，无转换逻辑)                        │   │
│   │  - BlacklistCache (黑名单缓存)                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心代码分析

#### 2.2.1 网关服务 - 状态机控制

**文件**: `gateway-service/src/main/java/com/network/gateway/cache/IpAttackStateCache.java`

```java
public StateTransitionResult checkAndTransitionState(String ip, int rateLimitCount, 
        int suspiciousThreshold, int attackingThreshold) {
    // 网关本地自动进行状态转换
    switch (currentState) {
        case NORMAL:
            if (rateLimitCount >= suspiciousThreshold) → SUSPICIOUS
        case SUSPICIOUS:
            if (rateLimitCount >= attackingThreshold) → ATTACKING
        case ATTACKING:
            if (持续攻击>30秒) → DEFENDED
        case DEFENDED:
            if (攻击停止) → COOLDOWN
        case COOLDOWN:
            if (冷却期结束) → NORMAL
    }
}
```

**分析**：

- ✅ 状态机逻辑完整，符合混合架构第一层防御要求
- ✅ 本地决策，实时响应

#### 2.2.2 网关服务 - 限流过滤器

**文件**: `gateway-service/src/main/java/com/network/gateway/filter/defense/RequestRateLimitFilter.java`

```java
public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
    // 1. 检查当前状态
    int currentState = attackStateCache.getState(sourceIp);
    
    // 2. DEFENDED状态直接拦截
    if (currentState == IpAttackStateConstant.DEFENDED) {
        return handleDefendedRequest(exchange, sourceIp, startTime);
    }
    
    // 3. 限流计数
    int currentRateLimitCount = attackStateCache.incrementRateLimitCount(sourceIp, windowMs);
    
    // 4. 状态转换
    StateTransitionResult transitionResult = 
        attackStateCache.checkAndTransitionState(sourceIp, currentRateLimitCount, ...);
    
    // 5. 状态转换后推送事件到监测服务
    if (transitionResult.isTransitioned()) {
        if (newState == ATTACKING) {
            pushDDoSEvent(sourceIp, ...);  // 推送DDoS事件
        } else if (newState == DEFENDED) {
            pushDDoSEvent(sourceIp, ...);
            pushBlacklistEvent(sourceIp, ...);  // 推送黑名单事件
        }
    }
}
```

**分析**：

- ✅ 实时限流检测
- ✅ 自动状态转换
- ✅ 事件推送到监测服务
- ⚠️ 推送事件后，监测服务可能再次生成防御指令（循环）

#### 2.2.3 监测服务 - DDoS检测

**文件**: `monitor-service/src/main/java/com/network/monitor/service/impl/DDoSDetectServiceImpl.java`

```java
public AttackMonitorDTO detect(TrafficMonitorDTO trafficDTO) {
    // 监测服务也有独立的DDoS检测逻辑！
    
    // 1. 独立的请求计数器
    AtomicInteger count = requestCounter.computeIfAbsent(counterKey, k -> new AtomicInteger(0));
    int currentCount = count.incrementAndGet();
    
    // 2. 独立的阈值判断
    if (currentCount > ddosThreshold) {
        // 3. 独立的状态更新
        attackStateCache.markAsAttacking(sourceIp);
        
        // 4. 返回攻击记录，触发防御决策
        return buildDDoSAttackDTO(...);
    }
}
```

**分析**：

- ❌ **与网关DDoS检测逻辑重复**
- ❌ 独立的请求计数器，与网关不同步
- ❌ 可能产生重复的防御决策

#### 2.2.4 监测服务 - 防御决策

**文件**: `monitor-service/src/main/java/com/network/monitor/service/impl/DefenseDecisionServiceImpl.java`

```java
public DefenseCommandDTO generateDefenseDecision(AttackMonitorDTO attackDTO) {
    // 1. 检查是否已防御
    if (attackStateCache.isInDefendedState(sourceIp)) {
        return null;  // 跳过
    }
    
    // 2. 根据风险等级生成防御指令
    if (riskLevel == HIGH || riskLevel == CRITICAL) {
        commandDTO = buildDefenseCommand(..., BLACKLIST, ...);
    }
    
    // 3. 推送防御指令到网关
    boolean success = gatewayApiClient.pushDefenseCommand(commandDTO);
    
    // 4. 记录防御日志
    recordDefenseLog(commandDTO, event, attackDTO);
    
    // 5. 发布黑名单同步事件
    publishBlacklistSyncEvent(commandDTO);
    
    // 6. 更新本地状态
    attackStateCache.markAsDefended(sourceIp, eventId);
}
```

**分析**：

- ✅ 完整的防御决策流程
- ✅ 记录防御日志和黑名单同步
- ⚠️ 网关已标记DEFENDED后，监测服务再推送指令（可能重复）

### 2.3 通信机制分析

#### 2.3.1 网关 → 监测服务

| 接口路径                           | 用途       | 触发时机                    |
| ------------------------------ | -------- | ----------------------- |
| `/api/inner/traffic/receive`   | 流量数据推送   | 每个请求（根据策略）              |
| `/api/inner/attack/ddos-event` | DDoS攻击事件 | 状态转换为ATTACKING/DEFENDED |
| `/api/inner/blacklist/event`   | 黑名单事件    | 状态转换为DEFENDED           |
| `/api/inner/defense/log/sync`  | 防御日志     | 每次防御操作                  |

#### 2.3.2 监测服务 → 网关

| 接口路径                              | 用途   | 触发时机    |
| --------------------------------- | ---- | ------- |
| `/api/gateway/defense/command`    | 防御指令 | 防御决策生成后 |
| `/api/gateway/defense/state/sync` | 状态同步 | 手动/定时同步 |
| `/api/gateway/config/sync`        | 配置同步 | 配置变更后   |

***

## 三、问题分析

### 3.1 问题清单

| 编号 | 问题                | 影响          | 严重程度 |
| -- | ----------------- | ----------- | ---- |
| P1 | 双重DDoS检测逻辑        | 资源浪费、可能重复决策 | 高    |
| P2 | 网关已决策后监测服务仍推送指令   | 重复操作、日志冗余   | 中    |
| P3 | 监测服务状态缓存无实际作用     | 代码冗余        | 低    |

### 3.2 数据流问题分析

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        当前数据流问题                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   场景：DDoS攻击检测                                                         │
│                                                                             │
│   网关检测到攻击                                                             │
│   ─────────────────────────────────────────────────────────────────────────  │
│   RequestRateLimitFilter:                                                   │
│   1. 限流计数超过阈值                                                        │
│   2. 状态转换: ATTACKING → DEFENDED                                         │
│   3. 推送DDoS事件到监测服务                                                  │
│   4. 推送黑名单事件到监测服务                                                │
│   5. 本地添加黑名单                                                          │
│                                                                             │
│   监测服务接收事件                                                           │
│   ─────────────────────────────────────────────────────────────────────────  │
│   AttackEventReceiveController.receiveDDoSEvent():                          │
│   1. 保存攻击记录                                                            │
│   2. 调用 DefenseDecisionService.generateDefenseDecision()                  │
│                                                                             │
│   DefenseDecisionServiceImpl:                                               │
│   1. 检查IP状态 (此时网关已标记DEFENDED)                                      │
│   2. 如果检查通过 (可能存在时间窗口)                                          │
│   3. 生成防御指令                                                            │
│   4. 推送防御指令到网关 ← 重复!                                              │
│   5. 记录防御日志                                                            │
│   6. 发布黑名单同步事件 ← 重复!                                              │
│                                                                             │
│   问题:                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ • 网关已执行防御，监测服务仍可能推送重复指令                           │   │
│   │ • 黑名单事件被发布两次（网关推送 + 监测服务发布）                       │   │
│   │ • 防御日志可能重复记录                                                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

***

## 四、优化方案设计

### 4.1 优化目标

1. **消除重复检测**：DDoS检测逻辑统一由网关负责
2. **明确职责边界**：网关负责实时防御，监测服务负责分析记录
3. **简化通信机制**：单网关直接通信，无需广播机制

### 4.2 优化后架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        优化后架构                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                              请求流量                                        │
│                                 │                                           │
│                                 ▼                                           │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         网关服务 (9000)                              │   │
│   │                                                                     │   │
│   │  第一层防御（本地实时）                                               │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  • 限流检测 ──> 本地计数，毫秒级响应                                 │   │
│   │  • 黑名单拦截 ──> 本地缓存，直接拦截                                 │   │
│   │  • 状态机 ──> 本地状态转换                                          │   │
│   │  • 事件推送 ──> 推送攻击事件到监测服务                               │   │
│   │                                                                     │   │
│   │  接收指令:                                                          │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  • 接收防御指令（手动处置场景）                                       │   │
│   │  • 接收黑名单同步                                                   │   │
│   │  • 接收配置同步                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                 │                                           │
│                    ┌────────────┼────────────┐                              │
│                    │ 推送事件   │ 推送流量   │ 接收指令                      │
│                    ▼            ▼            ▲                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         监测服务 (9002)                              │   │
│   │                                                                     │   │
│   │  第二层防御（分析记录）                                               │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  • 攻击事件聚合 ──> 合并重复事件                                     │   │
│   │  • 攻击记录存储 ──> 持久化攻击数据                                   │   │
│   │  • 态势感知 ──> 攻击态势展示                                         │   │
│   │  • 告警通知 ──> 邮件/飞书通知                                        │   │
│   │                                                                     │   │
│   │  手动处置:                                                          │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  • 手动封禁/解封 ──> 推送指令到网关                                  │   │
│   │  • 配置管理 ──> 推送配置到网关                                       │   │
│   │                                                                     │   │
│   │  移除: DDoSDetectServiceImpl (重复逻辑)                              │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 职责划分

| 层级      | 组件   | 职责                   | 决策权  |
| ------- | ---- | -------------------- | ---- |
| **第一层** | 网关服务 | 实时限流、黑名单拦截、状态转换、事件推送 | 本地决策 |
| **第二层** | 监测服务 | 事件聚合、数据存储、态势感知、告警通知  | 手动处置 |

### 4.4 具体优化项

#### 4.4.1 移除监测服务DDoS检测逻辑

**问题**: `DDoSDetectServiceImpl` 与网关限流检测逻辑重复

**方案**:

- 移除监测服务的独立DDoS检测
- 保留接口用于接收网关推送的攻击事件
- 监测服务只负责记录和分析，不再进行二次检测

**涉及文件**:

- `monitor-service/src/main/java/com/network/monitor/service/impl/DDoSDetectServiceImpl.java`
- `monitor-service/src/main/java/com/network/monitor/service/DDoSDetectService.java`

#### 4.4.2 优化事件处理流程

**问题**: 网关推送DDoS事件后，监测服务仍可能生成重复防御指令

**方案**:

```java
// AttackEventReceiveController.receiveDDoSEvent()
@PostMapping("/ddos-event")
public ApiResponse<Void> receiveDDoSEvent(@RequestBody Map<String, Object> eventData) {
    // 1. 检查IP是否已防御
    if (attackStateCache.isInDefendedState(sourceIp)) {
        log.info("IP已处于DEFENDED状态，仅记录事件: ip={}", sourceIp);
        // 仅保存攻击记录，不生成防御决策
        saveAttackRecord(eventData);
        return ApiResponse.success();
    }
    
    // 2. 保存攻击记录
    saveAttackRecord(eventData);
    
    // 3. 仅在需要时生成防御决策（如手动处置场景）
    // 自动防御由网关完成，此处不再重复
}
```

#### 4.4.3 优化防御日志记录

**问题**: 防御日志可能重复记录

**方案**: 增加去重逻辑，同一事件只记录一次

```java
// DefenseDecisionServiceImpl.recordDefenseLog()
private void recordDefenseLog(DefenseCommandDTO commandDTO, ...) {
    // 检查是否已记录
    if (defenseLogMapper.existsByEventId(commandDTO.getEventId())) {
        log.debug("防御日志已存在，跳过: eventId={}", commandDTO.getEventId());
        return;
    }
    
    // 记录防御日志
    DefenseLogEntity logEntity = new DefenseLogEntity();
    logEntity.setEventId(commandDTO.getEventId());
    logEntity.setIsFirst(1);
    // ... 其他字段
    defenseLogMapper.insert(logEntity);
}
```

***

## 五、流量采集与推送策略优化

### 5.1 当前策略分析

#### 5.1.1 过滤器执行顺序

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        过滤器执行顺序                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   优先级    过滤器名称                        作用                          │
│   ─────────────────────────────────────────────────────────────────────────  │
│   -100     TrafficCollectGlobalFilter       流量采集（最先执行）            │
│   0        IpBlacklistDefenseFilter         黑名单拦截                      │
│   5        DefenseStateCheckFilter          防御状态检查                    │
│   10       RequestRateLimitFilter           限流检测                        │
│   20       MaliciousRequestBlockFilter      恶意请求拦截                    │
│                                                                             │
│   ✅ 流量采集过滤器优先级最高(-100)，会采集所有流量                           │
│      包括被后续过滤器拦截的流量                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 5.1.2 当前推送策略

| IP状态       | 推送策略     | 说明              |
| ---------- | -------- | --------------- |
| NORMAL     | REALTIME | 实时推送每条流量        |
| SUSPICIOUS | SAMPLING | 采样推送（如每10条推送1条） |
| COOLDOWN   | SAMPLING | 采样推送            |
| ATTACKING  | BATCH    | 批量推送（定时聚合）      |
| DEFENDED   | BATCH    | 批量推送            |

#### 5.1.3 存在的问题

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        当前策略的问题                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   问题1: NORMAL状态实时推送压力大                                            │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 正常流量占绝大多数，实时推送会产生大量网络请求                             │
│   • 监测服务需要处理大量正常流量数据                                          │
│                                                                             │
│   问题2: 被拦截流量的推送策略不够优化                                         │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 黑名单拦截的流量（DEFENDED）使用BATCH，但可能仍占用大量带宽                │
│   • DDoS攻击时，被拦截的流量可能达到数万条/秒                                 │
│                                                                             │
│   问题3: 缺少对"被拦截流量"的特殊处理                                        │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 被拦截的流量有特殊标记（如状态码403/429）                                 │
│   • 这些流量的分析价值不同，应该有不同的推送策略                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 优化后的推送策略

#### 5.2.1 核心原则

1. **完整采集**：所有流量都必须采集，包括被拦截的流量
2. **智能推送**：根据流量特征和系统负载动态调整推送策略
3. **数据聚合**：高频流量在网关侧聚合，减少推送量
4. **优先级区分**：异常流量优先推送，正常流量延迟推送

#### 5.2.2 优化后的策略定义

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        优化后的推送策略                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   流量类型              │ 推送策略        │ 说明                            │
│   ─────────────────────┼────────────────┼───────────────────────────────── │
│   正常流量(NORMAL)      │ DELAYED_BATCH  │ 延迟批量，每5秒聚合推送一次       │
│   可疑流量(SUSPICIOUS)  │ SAMPLING       │ 采样推送，每N条推送1条            │
│   攻击流量(ATTACKING)   │ AGGREGATE      │ 聚合推送，统计信息+关键样本       │
│   被拦截流量(DEFENDED)  │ COUNTER_ONLY   │ 仅计数，定时推送统计信息          │
│   冷却期流量(COOLDOWN)  │ SAMPLING       │ 采样推送                         │
│                                                                             │
│   特殊情况:                                                                 │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 首次攻击检测到时 → 立即推送关键样本                                      │
│   • 攻击升级时 → 立即推送告警                                                │
│   • 手动处置时 → 立即推送相关流量                                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 5.2.3 详细策略说明

**1. 正常流量（NORMAL）- 延迟批量推送**

```json
{
  "aggregateType": "NORMAL_TRAFFIC",
  "timeWindow": "2026-03-25 10:00:00 - 10:00:05",
  "totalRequests": 1500,
  "uniqueIps": 120,
  "avgResponseTime": 45,
  "errorRate": 0.02,
  "topUris": ["/api/xxx", "/api/yyy"],
  "httpMethods": {"GET": 1000, "POST": 500}
}
```

**2. 被拦截流量（DEFENDED）- 仅计数推送**

```json
{
  "aggregateType": "BLOCKED_TRAFFIC",
  "blockedIp": "192.168.1.100",
  "eventId": "EVT_xxx",
  "blockReason": "BLACKLIST",
  "timeWindow": "2026-03-25 10:00:00 - 10:00:10",
  "totalBlocked": 5000,
  "peakRps": 520,
  "topUris": ["/api/xxx"],
  "sampleRequest": {
    "uri": "/api/xxx",
    "method": "GET",
    "userAgent": "Mozilla/5.0...",
    "time": "2026-03-25 10:00:01"
  }
}
```

**3. 攻击流量（ATTACKING）- 聚合推送**

```json
{
  "aggregateType": "ATTACK_TRAFFIC",
  "sourceIp": "192.168.1.100",
  "eventId": "EVT_xxx",
  "attackType": "DDOS",
  "timeWindow": "2026-03-25 10:00:00 - 10:00:02",
  "totalRequests": 2000,
  "avgRps": 1000,
  "peakRps": 1200,
  "sampleRequests": [
    {"uri": "/api/xxx", "method": "GET", "time": "..."},
    {"uri": "/api/yyy", "method": "POST", "time": "..."}
  ]
}
```

### 5.3 数据量对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│              数据量对比（假设DDoS攻击10000请求/秒，持续30秒）                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   场景                    │ 当前策略          │ 优化后策略                   │
│   ───────────────────────┼──────────────────┼─────────────────────────────  │
│   正常流量(1000/秒)       │ 30000条实时推送   │ 6条聚合推送(每5秒1条)         │
│   攻击流量(9000/秒)       │ 270000条批量推送  │ 15条聚合推送(每2秒1条)        │
│   被拦截流量(9000/秒)     │ 270000条批量推送  │ 3条统计推送(每10秒1条)        │
│   ───────────────────────┼──────────────────┼─────────────────────────────  │
│   总推送量                │ ~570000条        │ ~24条                        │
│   数据压缩比              │ 1x               │ ~24000x                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.4 实现建议

| 序号  | 任务         | 涉及文件                       | 说明                                          |
| --- | ---------- | -------------------------- | ------------------------------------------- |
| 5.1 | 新增推送策略枚举   | TrafficPushStrategy        | 增加 DELAYED_BATCH, COUNTER_ONLY, AGGREGATE |
| 5.2 | 优化推送逻辑     | TrafficCollectGlobalFilter | 根据流量类型选择不同策略                                |
| 5.3 | 增加聚合数据结构   | TrafficAggregateDTO        | 统计数据 + 关键样本                                 |
| 5.4 | 增加关键事件立即推送 | RequestRateLimitFilter     | 首次攻击、攻击升级等                                  |

***

## 六、告警机制分析与优化

### 6.1 当前告警机制

#### 6.1.1 现有实现

**文件**: `monitor-service/src/main/java/com/network/monitor/controller/outer/AttackMonitorController.java`

```java
@GetMapping("/alert/realtime")
public void getRealTimeAlerts(...) {
    // 使用SSE推送未处理的高危告警
    response.setContentType("text/event-stream");
    while (System.currentTimeMillis() - startTime < timeout) {
        List<AttackMonitorEntity> alerts = attackMonitorMapper.selectUnHandledHighRisk();
        if (alerts != null && !alerts.isEmpty()) {
            response.getWriter().write("data: " + toJson(alerts) + "\n\n");
        }
    }
}
```

#### 6.1.2 存在的问题

| 问题       | 影响                 | 严重程度 |
| -------- | ------------------ | ---- |
| 无告警分级    | 所有高危告警一起推送，无法区分优先级 | 中    |
| 无告警抑制    | 同一攻击事件可能产生大量重复告警   | 高    |
| 无通知渠道    | 仅前端展示，无邮件/飞书等通知    | 高    |
| 无告警确认机制  | 告警无法标记为已确认/已处理     | 中    |
| 无告警历史    | 无法查看历史告警记录和统计      | 低    |

### 6.2 告警机制设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        告警机制架构                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   告警生成层                                                                 │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 攻击检测 → 生成告警事件                                                  │
│   • 状态转换 → 生成状态变更告警                                              │
│   • 系统异常 → 生成系统告警                                                  │
│                                                                             │
│   告警处理层                                                                 │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 告警聚合：同一事件合并为一条告警                                          │
│   • 告警抑制：短时间内相同告警只发送一次                                      │
│   • 告警静默：维护期间暂停告警                                               │
│                                                                             │
│   告警通知层                                                                 │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 前端推送：SSE实时推送                                                    │
│   • 邮件通知：SMTP                                                          │
│   • 飞书通知：飞书机器人Webhook                                              │
│                                                                             │
│   告警管理层                                                                 │
│   ─────────────────────────────────────────────────────────────────────────  │
│   • 告警确认：标记为已确认/误报                                              │
│   • 告警处理：关联处置记录                                                   │
│   • 告警统计：告警趋势、响应时间统计                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.3 优化建议

#### 6.3.1 新增告警表结构

```sql
CREATE TABLE `sys_alert` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `alert_id` VARCHAR(64) NOT NULL COMMENT '告警唯一标识',
    `event_id` VARCHAR(64) COMMENT '关联事件ID',
    `alert_type` VARCHAR(32) NOT NULL COMMENT '告警类型(ATTACK/DDOS/SYSTEM)',
    `alert_level` VARCHAR(20) NOT NULL COMMENT '告警级别(CRITICAL/HIGH/MEDIUM/LOW)',
    `alert_title` VARCHAR(255) NOT NULL COMMENT '告警标题',
    `alert_content` TEXT COMMENT '告警内容',
    `source_ip` VARCHAR(128) COMMENT '源IP',
    `attack_type` VARCHAR(32) COMMENT '攻击类型',
    `status` TINYINT DEFAULT 0 COMMENT '状态(0-待处理,1-已确认,2-已处理,3-已忽略)',
    `suppress_count` INT DEFAULT 0 COMMENT '抑制次数',
    `first_alert_time` DATETIME NOT NULL COMMENT '首次告警时间',
    `last_alert_time` DATETIME COMMENT '最后告警时间',
    `confirm_time` DATETIME COMMENT '确认时间',
    `confirm_user` VARCHAR(64) COMMENT '确认人',
    `handle_time` DATETIME COMMENT '处理时间',
    `handle_user` VARCHAR(64) COMMENT '处理人',
    `handle_remark` TEXT COMMENT '处理备注',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_alert_id` (`alert_id`),
    KEY `idx_event_id` (`event_id`),
    KEY `idx_status` (`status`),
    KEY `idx_alert_level` (`alert_level`)
) COMMENT='告警记录表';
```

#### 6.3.2 告警规则配置

```sql
CREATE TABLE `sys_alert_rule` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `rule_name` VARCHAR(100) NOT NULL COMMENT '规则名称',
    `alert_type` VARCHAR(32) NOT NULL COMMENT '告警类型',
    `alert_level` VARCHAR(20) NOT NULL COMMENT '告警级别',
    `condition_expr` TEXT COMMENT '触发条件表达式',
    `suppress_duration` INT DEFAULT 300 COMMENT '抑制时长(秒)',
    `notify_channels` VARCHAR(255) COMMENT '通知渠道(SSE,EMAIL,FEISHU)',
    `notify_users` VARCHAR(500) COMMENT '通知用户列表',
    `enabled` TINYINT DEFAULT 1 COMMENT '是否启用',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`)
) COMMENT='告警规则配置表';
```

#### 6.3.3 实现建议

| 序号  | 任务       | 说明                       |
| --- | -------- | ------------------------ |
| 6.1 | 新增告警服务   | AlertService，负责告警生成、聚合、抑制 |
| 6.2 | 新增邮件通知   | 支持SMTP邮件发送               |
| 6.3 | 新增飞书通知   | 飞书机器人Webhook             |
| 6.4 | 增加告警确认   | 前端可确认/忽略告警               |
| 6.5 | 增加告警统计   | 告警趋势、响应时间统计              |

***

## 七、溯源能力分析与优化

### 7.1 当前溯源能力

#### 7.1.1 现有数据表

| 表名                   | 用途   | 溯源能力         |
| -------------------- | ---- | ------------ |
| sys_traffic_monitor  | 流量记录 | ✅ 可追溯请求详情   |
| sys_attack_monitor   | 攻击记录 | ✅ 可追溯攻击详情   |
| sys_attack_event     | 攻击事件 | ✅ 可追溯事件聚合   |
| sys_defense_log      | 防御日志 | ✅ 可追溯防御操作   |
| sys_ip_blacklist     | 黑名单  | ✅ 可追溯封禁记录   |

#### 7.1.2 现有关联关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        当前数据关联关系                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   sys_attack_event (事件)                                                   │
│         │                                                                   │
│         │ event_id                                                          │
│         ▼                                                                   │
│   sys_attack_monitor (攻击记录)                                             │
│         │                                                                   │
│         │ traffic_id                                                        │
│         ▼                                                                   │
│   sys_traffic_monitor (流量记录)                                            │
│         │                                                                   │
│         │ id                                                                │
│         ▼                                                                   │
│   sys_defense_log (防御日志)                                                │
│                                                                             │
│   ✅ 已实现基本关联，可通过事件ID追溯完整攻击链                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 存在的问题

| 问题        | 影响             | 严重程度 |
| --------- | -------------- | ---- |
| 无IP历史攻击查询 | 无法查看某IP的历史攻击记录 | 中    |
| 无攻击链可视化   | 无法直观展示攻击时间线    | 低    |
| 无请求体完整保存  | 大请求体被截断，影响分析   | 中    |
| 无地理位置信息   | 无法展示攻击来源地理分布   | 低    |

### 7.3 优化建议

#### 7.3.1 增加IP画像功能

```sql
CREATE TABLE `sys_ip_profile` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `ip` VARCHAR(128) NOT NULL COMMENT 'IP地址(规范化)',
    `ip_version` TINYINT DEFAULT 4 COMMENT 'IP版本(4/6)',
    `country` VARCHAR(64) COMMENT '国家',
    `province` VARCHAR(64) COMMENT '省份',
    `city` VARCHAR(64) COMMENT '城市',
    `isp` VARCHAR(128) COMMENT '运营商',
    `risk_score` INT DEFAULT 0 COMMENT '风险评分(0-100)',
    `attack_count` INT DEFAULT 0 COMMENT '攻击次数',
    `first_seen` DATETIME COMMENT '首次出现时间',
    `last_seen` DATETIME COMMENT '最后出现时间',
    `tags` VARCHAR(255) COMMENT '标签(代理/VPN/IDC等)',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_ip` (`ip`)
) COMMENT='IP画像表';
```

#### 7.3.2 实现建议

| 序号  | 任务        | 说明                 |
| --- | --------- | ------------------ |
| 7.1 | 增加IP画像服务  | 集成IP地理位置库，记录IP历史行为 |
| 7.2 | 增加溯源查询API | 支持按IP/事件/时间范围查询    |
| 7.3 | 增加攻击链可视化  | 前端展示攻击时间线          |

***

## 八、其他优化建议

### 8.1 数据生命周期管理

#### 8.1.1 当前问题

| 问题        | 影响       |
| --------- | -------- |
| 流量数据无清理机制 | 数据库持续膨胀  |
| 攻击数据无归档机制 | 历史数据查询变慢 |
| 无数据保留策略配置 | 无法满足合规要求 |

#### 8.1.2 优化建议

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        数据生命周期策略                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   数据类型          │ 热数据    │ 温数据      │ 冷数据      │ 归档/删除    │
│   ─────────────────┼──────────┼────────────┼────────────┼─────────────  │
│   流量数据          │ 1天      │ 7天        │ 30天       │ 90天删除      │
│   攻击记录          │ 7天      │ 30天       │ 90天       │ 365天归档     │
│   攻击事件          │ 30天     │ 90天       │ 365天      │ 永久保留      │
│   防御日志          │ 7天      │ 30天       │ 90天       │ 365天归档     │
│   告警记录          │ 7天      │ 30天       │ 90天       │ 365天归档     │
│                                                                             │
│   实现方式:                                                                 │
│   • 热数据: MySQL主表                                                        │
│   • 温数据: MySQL分区表                                                      │
│   • 冷数据: 归档到文件                                                        │
│   • 删除: 定时任务清理                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 8.2 性能优化

#### 8.2.1 数据库优化

| 优化项   | 说明              |
| ----- | --------------- |
| 分区表   | 流量表按时间分区，提升查询性能 |
| 索引优化  | 增加复合索引，减少全表扫描   |
| 连接池优化 | 增加连接池大小，减少连接开销  |

#### 8.2.2 缓存优化

| 优化项     | 说明                         |
| ------- | -------------------------- |
| 本地缓存    | Caffeine缓存，减少数据库压力        |
| 缓存预热    | 系统启动时预加载热点数据               |

### 8.3 安全加固

#### 8.3.1 当前风险

| 风险       | 影响          |
| -------- | ----------- |
| 跨服务调用无加密 | 数据传输可能被窃听   |
| 无操作审计日志  | 无法追溯操作记录    |
| 无权限控制    | 任何用户可访问所有功能 |

#### 8.3.2 优化建议

| 优化项     | 说明             |
| ------- | -------------- |
| HTTPS加密 | 所有跨服务调用使用HTTPS |
| 操作审计    | 记录所有敏感操作日志     |
| RBAC权限  | 基于角色的访问控制      |
| 接口限流    | 防止API被滥用       |

***

## 九、系统配置项

### 9.1 现有配置项

系统配置存储在 `sys_config` 表中，以下是当前已有的配置项：

#### 9.1.1 基础配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| blacklist.default.expire.seconds | 86400 | 黑名单默认过期时间（秒） |
| alert.enabled | true | 是否启用告警通知 |
| alert.push.interval | 5000 | 告警推送间隔（毫秒） |
| alert.heartbeat.interval | 10000 | 告警心跳间隔（毫秒） |

#### 9.1.2 AI模型配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| ai.model.url | (空) | AI大模型接口地址 |
| ai.model.apiKey | (空) | AI大模型API密钥 |

#### 9.1.3 DDoS防护配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| ddos.threshold | 20 | DDoS检测阈值(次/秒) |
| ddos.detection.window-ms | 1000 | DDoS检测时间窗口(毫秒) |
| ddos.rate-limit-trigger-count | 3 | 连续限流触发封禁阈值(次) |
| ddos.rate-limit-trigger-window-seconds | 60 | 连续限流检测时间窗口(秒) |
| ddos.peak-rps.record.enabled | true | 是否记录峰值RPS |

#### 9.1.4 防御策略配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| defense.blacklist.default-duration-seconds | 1800 | 黑名单默认持续时间(秒) |
| defense.blacklist.high-risk-duration-seconds | 3600 | 高风险攻击封禁时长(秒) |
| defense.blacklist.critical-risk-duration-seconds | 86400 | 严重风险攻击封禁时长(秒) |
| defense.blacklist.auto-unban.enabled | true | 是否启用自动解封 |
| defense.blacklist.repeat-offender.multiplier | 2 | 重复违规封禁时长倍数 |
| defense.blacklist.repeat-offender.max-multiplier | 8 | 最大封禁时长倍数 |

#### 9.1.5 流量推送策略配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| traffic.push.normal.strategy | realtime | NORMAL状态推送策略: realtime/sampling |
| traffic.push.suspicious.strategy | sampling | SUSPICIOUS状态推送策略 |
| traffic.push.attacking.strategy | batch | ATTACKING状态推送策略 |
| traffic.push.defended.strategy | skip | DEFENDED状态推送策略: skip/sampling |
| traffic.push.cooldown.strategy | sampling | COOLDOWN状态推送策略 |
| traffic.push.batch-interval-ms | 5000 | 批量推送间隔(毫秒) |
| traffic.push.sampling-rate | 10 | 采样推送比例(1/N) |
| traffic.push.enabled | true | 是否启用流量推送 |

#### 9.1.6 网关防御配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| gateway.defense.blacklist.enabled | true | 网关-黑名单防御开关 |
| gateway.defense.rate-limit.enabled | true | 网关-限流防御开关 |
| gateway.defense.malicious-request.enabled | true | 网关-恶意请求拦截开关 |
| gateway.defense.rate-limit.default-threshold | 10 | 网关-默认限流阈值(次/秒) |
| gateway.defense.rate-limit.window-size | 1000 | 网关-限流时间窗口(毫秒) |
| gateway.defense.blacklist.default-expire-seconds | 600 | 网关-黑名单默认过期时间(秒) |

#### 9.1.7 网关恶意请求检测配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| gateway.defense.malicious.user-agents | sqlmap,nessus,nmap... | 网关-恶意User-Agent列表(逗号分隔) |
| gateway.defense.malicious.uri-patterns | /admin,/manager... | 网关-恶意URI模式列表(逗号分隔) |

#### 9.1.8 网关缓存配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| gateway.cache.traffic-expire-ms | 3600000 | 网关-流量缓存过期时间(毫秒) |
| gateway.cache.blacklist-expire-ms | 600000 | 网关-黑名单缓存过期时间(毫秒) |
| gateway.cache.cleanup-interval-ms | 60000 | 网关-缓存清理间隔(毫秒) |

#### 9.1.9 网关攻击状态配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| gateway.attack-state.cooldown-duration-ms | 300000 | 网关-冷却持续时间(毫秒) |
| gateway.attack-state.state-expire-ms | 600000 | 网关-攻击状态过期时间(毫秒) |

#### 9.1.10 网关请求限制配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| gateway.request.max-body-size | 102400 | 网关-最大请求体大小(字节) |
| gateway.request.abnormal-response-threshold-ms | 3000 | 网关-异常响应时间阈值(毫秒) |

### 9.2 新增配置项建议

根据优化方案，建议新增以下配置项：

#### 9.2.1 告警通知配置

```sql
INSERT INTO `sys_config` (`config_key`, `config_value`, `description`) VALUES
('alert.email.enabled', 'false', '是否启用邮件通知'),
('alert.email.smtp-host', '', 'SMTP服务器地址'),
('alert.email.smtp-port', '465', 'SMTP服务器端口'),
('alert.email.smtp-ssl', 'true', '是否启用SSL'),
('alert.email.username', '', 'SMTP用户名'),
('alert.email.password', '', 'SMTP密码'),
('alert.email.from', '', '发件人地址'),
('alert.email.to', '', '收件人地址(逗号分隔)'),
('alert.feishu.enabled', 'false', '是否启用飞书通知'),
('alert.feishu.webhook-url', '', '飞书机器人Webhook地址'),
('alert.feishu.secret', '', '飞书机器人签名密钥'),
('alert.suppress.duration-seconds', '300', '告警抑制时长(秒)'),
('alert.level.notify', 'HIGH,CRITICAL', '触发通知的告警级别');
```

#### 9.2.2 流量推送策略优化配置

```sql
INSERT INTO `sys_config` (`config_key`, `config_value`, `description`) VALUES
('traffic.push.normal.batch-interval-ms', '5000', 'NORMAL状态批量推送间隔(毫秒)'),
('traffic.push.defended.counter-interval-ms', '10000', 'DEFENDED状态计数推送间隔(毫秒)'),
('traffic.push.attacking.aggregate-interval-ms', '2000', 'ATTACKING状态聚合推送间隔(毫秒)'),
('traffic.push.sample-size', '5', '聚合推送时保留的样本数量');
```

#### 9.2.3 IP画像配置

```sql
INSERT INTO `sys_config` (`config_key`, `config_value`, `description`) VALUES
('ip-profile.geo-db.enabled', 'true', '是否启用IP地理位置查询'),
('ip-profile.geo-db.path', 'data/GeoLite2-City.mmdb', 'IP地理位置数据库路径'),
('ip-profile.risk-score.threshold', '50', 'IP风险评分阈值(超过则标记高风险)');
```

#### 9.2.4 数据生命周期配置

```sql
INSERT INTO `sys_config` (`config_key`, `config_value`, `description`) VALUES
('data.retention.traffic.days', '30', '流量数据保留天数'),
('data.retention.attack.days', '90', '攻击记录保留天数'),
('data.retention.event.days', '365', '攻击事件保留天数'),
('data.retention.defense-log.days', '90', '防御日志保留天数'),
('data.retention.alert.days', '90', '告警记录保留天数'),
('data.cleanup.enabled', 'true', '是否启用数据自动清理'),
('data.cleanup.cron', '0 0 3 * * ?', '数据清理定时任务Cron表达式');
```

### 9.3 配置项管理建议

| 建议 | 说明 |
|------|------|
| 配置分组 | 将配置项按功能分组，便于管理 |
| 配置热更新 | 支持配置项热更新，无需重启服务 |
| 配置验证 | 配置变更时进行格式和范围验证 |
| 配置审计 | 记录配置变更历史，便于追溯 |
| 默认值管理 | 提供配置项默认值，防止配置缺失 |

***

## 十、实施计划

> **状态说明**：`[ ]` 待完成 / `[x]` 已完成 / `[-]` 进行中

---

### 10.1 阶段一：消除重复逻辑（高优先级）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**目标**：消除网关与监测服务之间的重复检测逻辑，明确职责边界

#### 10.1.1 移除监测服务DDoS检测逻辑

- [x] 1.1.1 分析 `DDoSDetectServiceImpl` 的调用关系
  - 检查哪些Controller/Service调用了DDoS检测方法
  - 记录调用位置和用途
  - 涉及文件：`DDoSDetectServiceImpl.java`, `DDoSDetectService.java`

- [x] 1.1.2 移除DDoS检测服务实现
  - 注释或删除 `DDoSDetectServiceImpl` 中的检测逻辑
  - 保留接口定义，抛出 `UnsupportedOperationException`
  - 涉及文件：`DDoSDetectServiceImpl.java`

- [x] 1.1.3 移除相关调用代码
  - 移除Controller中的DDoS检测调用
  - 移除定时任务中的DDoS检测调用
  - 涉及文件：相关Controller、定时任务类

- [x] 1.1.4 更新单元测试
  - 移除或更新DDoS检测相关的单元测试
  - 涉及文件：`DDoSDetectServiceImplTest.java`

#### 10.1.2 优化攻击事件接收逻辑

- [x] 1.2.1 分析事件接收流程
  - 梳理 `AttackEventReceiveController` 的处理逻辑
  - 确认事件接收后的处理流程
  - 涉及文件：`AttackEventReceiveController.java`

- [x] 1.2.2 移除重复的防御决策生成
  - 检查是否在事件接收后重复生成防御决策
  - 移除重复的防御指令推送逻辑
  - 涉及文件：`AttackEventReceiveController.java`, `DefenseDecisionServiceImpl.java`

- [x] 1.2.3 简化事件处理流程
  - 事件接收后仅记录到数据库
  - 不再触发额外的检测或决策逻辑
  - 涉及文件：`AttackEventReceiveController.java`

#### 10.1.3 防御日志去重

- [x] 1.3.1 分析防御日志写入逻辑
  - 检查 `DefenseLogMapper` 的插入逻辑
  - 确认日志写入的触发点
  - 涉及文件：`DefenseLogMapper.java`, `DefenseLogMapper.xml`

- [x] 1.3.2 实现日志去重机制
  - 基于 `event_id` 进行去重判断
  - 添加唯一索引或应用层去重
  - 涉及文件：`DefenseLogMapper.xml`, 数据库表结构

- [x] 1.3.3 更新数据库索引
  - 为 `sys_defense_log` 表添加 `event_id` 唯一索引
  - 涉及文件：数据库初始化脚本 `init_all.sql`

#### 10.1.4 阶段验收

- [x] 1.4.1 功能测试
  - 验证DDoS攻击仅由网关检测
  - 验证事件接收后无重复处理
  - 验证防御日志无重复记录

- [x] 1.4.2 性能测试
  - 对比优化前后的响应时间
  - 记录性能提升数据

---

### 10.2 阶段二：优化流量推送策略（中优先级）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**目标**：根据IP状态实现差异化流量推送策略，减少攻击时的推送量

#### 10.2.1 扩展推送策略枚举

- [x] 2.1.1 新增策略枚举值
  - 在 `TrafficPushStrategy` 中添加 `DELAYED_BATCH`, `COUNTER_ONLY`, `AGGREGATE`
  - 涉及文件：`TrafficPushStrategy.java`

- [x] 2.1.2 定义策略接口
  - 为每种策略定义处理接口
  - 涉及文件：`TrafficPushStrategy.java`, `TrafficPushHandler.java`

#### 10.2.2 实现延迟批量推送（NORMAL状态）

- [x] 2.2.1 创建延迟批量推送处理器
  - 实现5秒窗口内的流量聚合
  - 聚合后一次性推送
  - 涉及文件：`DelayedBatchPushHandler.java`

- [x] 2.2.2 添加定时任务
  - 创建定时任务定期推送聚合数据
  - 涉及文件：`TrafficCollectGlobalFilter.java`

#### 10.2.3 实现计数推送（DEFENDED状态）

- [x] 2.3.1 创建计数推送处理器
  - 仅统计请求数量，不保存详细流量
  - 定期推送统计数据
  - 涉及文件：`CounterOnlyPushHandler.java`

- [x] 2.3.2 实现计数器
  - 使用本地缓存记录请求计数
  - 涉及文件：`CounterOnlyPushHandler.java`

#### 10.2.4 实现聚合推送（ATTACKING状态）

- [x] 2.4.1 创建聚合推送处理器
  - 统计：总请求数、峰值RPS、平均响应时间
  - 样本：保留前5个关键请求样本
  - 涉及文件：`AggregatePushHandler.java`

- [x] 2.4.2 创建聚合数据结构
  - 定义 `TrafficAggregate` 内部类
  - 包含统计字段和样本列表
  - 涉及文件：`AggregatePushHandler.java`

#### 10.2.5 实现采样推送（SUSPICIOUS/COOLDOWN状态）

- [x] 2.5.1 创建采样推送处理器
  - 按配置的采样率（1/N）选择推送
  - 涉及文件：`SamplingPushHandler.java`

- [x] 2.5.2 实现采样算法
  - 基于时间或计数进行采样
  - 涉及文件：`SamplingPushHandler.java`

#### 10.2.6 整合推送策略选择逻辑

- [x] 2.6.1 更新流量收集过滤器
  - 在 `TrafficCollectGlobalFilter` 中根据IP状态选择策略
  - 涉及文件：`TrafficCollectGlobalFilter.java`

- [x] 2.6.2 添加策略配置读取
  - 创建 `TrafficPushStrategyManager` 管理策略处理器
  - 涉及文件：`TrafficPushStrategyManager.java`

#### 10.2.7 阶段验收

- [x] 2.7.1 功能测试
  - 验证各状态下的推送策略正确执行
  - 验证聚合数据结构正确

- [x] 2.7.2 性能测试
  - 模拟攻击场景，对比推送量
  - 目标：攻击时推送量减少99%以上

---

### 10.3 阶段三：完善告警机制（中优先级）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：1天  
**目标**：实现邮件和飞书告警通知，支持告警抑制和确认，完善前端告警页面

#### 10.3.1 数据库表结构设计

- [x] 3.1.1 创建告警表
  - 设计 `sys_alert` 表结构
  - 字段：告警ID、级别、状态、抑制标记等
  - 涉及文件：数据库迁移脚本

- [x] 3.1.2 创建告警规则表
  - 设计 `sys_alert_rule` 表结构
  - 字段：规则名称、触发条件、通知渠道等
  - 涉及文件：数据库迁移脚本

- [x] 3.1.3 添加告警配置项
  - 插入邮件和飞书相关配置项
  - 涉及文件：`init_all.sql`

#### 10.3.2 告警服务实现

- [x] 3.2.1 创建告警实体和Mapper
  - 创建 `AlertEntity`, `AlertRuleEntity`
  - 创建对应的Mapper接口和XML
  - 涉及文件：`AlertEntity.java`, `AlertMapper.java`

- [x] 3.2.2 实现告警生成服务
  - 根据攻击事件生成告警
  - 实现告警级别判断逻辑
  - 涉及文件：`AlertService.java`, `AlertServiceImpl.java`

- [x] 3.2.3 实现告警聚合服务
  - 合并相同IP的重复告警
  - 涉及文件：`AlertService.java`

- [x] 3.2.4 实现告警抑制服务
  - 在抑制时间内不重复发送通知
  - 涉及文件：`AlertSuppressService.java`

#### 10.3.3 邮件通知实现

- [x] 3.3.1 创建邮件配置类
  - 读取SMTP配置项
  - 涉及文件：`EmailConfig.java`

- [x] 3.3.2 实现邮件发送服务
  - 使用JavaMail或Spring Mail
  - 支持HTML格式邮件
  - 涉及文件：`EmailNotificationService.java`

- [x] 3.3.3 设计邮件模板
  - 告警通知邮件模板
  - 包含攻击详情和处理建议
  - 涉及文件：`alert-email-template.html`

#### 10.3.4 飞书通知实现

- [x] 3.4.1 创建飞书配置类
  - 读取Webhook URL和签名密钥
  - 涉及文件：`FeishuConfig.java`

- [x] 3.4.2 实现飞书消息发送
  - 调用飞书机器人Webhook
  - 支持签名验证
  - 涉及文件：`FeishuNotificationService.java`

- [x] 3.4.3 设计飞书消息卡片
  - 使用飞书消息卡片格式
  - 包含攻击详情和操作按钮
  - 涉及文件：`FeishuMessageBuilder.java`

#### 10.3.5 告警确认功能

- [x] 3.5.1 实现告警确认接口
  - 标记告警为已确认
  - 记录确认人和确认时间
  - 涉及文件：`AlertController.java`

- [x] 3.5.2 添加前端确认按钮
  - 在告警列表添加确认操作
  - 涉及文件：前端页面

#### 10.3.6 前端告警页面实现

- [x] 3.6.1 创建告警列表页面
  - 页面路径：`/alert/list`
  - 功能：展示告警列表，支持筛选、排序、分页
  - 筛选条件：告警级别、状态、时间范围、IP地址
  - 涉及文件：`alert-list.html`, `alert-list.js`

- [x] 3.6.2 实现告警列表表格
  - 表格列：告警ID、级别、来源IP、攻击类型、状态、时间、操作
  - 级别标识：CRITICAL(红色)、HIGH(橙色)、MEDIUM(黄色)、LOW(蓝色)
  - 状态标签：待处理、已确认、已忽略
  - 涉及文件：`alert-list.html`

- [x] 3.6.3 实现告警详情弹窗
  - 点击告警行显示详情弹窗
  - 详情内容：攻击详情、请求信息、防御动作、历史记录
  - 操作按钮：确认、忽略、查看溯源
  - 涉及文件：`alert-detail-modal.html`

- [x] 3.6.4 实现告警确认/忽略操作
  - 确认按钮：调用确认接口，更新状态
  - 忽略按钮：调用忽略接口，标记为已忽略
  - 批量操作：支持批量确认/忽略
  - 涉及文件：`alert-list.js`

- [x] 3.6.5 创建告警配置页面
  - 页面路径：`/alert/config`
  - 功能：配置告警规则、通知渠道、抑制策略
  - 配置项：邮件配置、飞书配置、告警阈值
  - 涉及文件：`alert-config.html`, `alert-config.js`

- [ ] 3.6.6 实现告警通知配置表单
  - 邮件配置：SMTP服务器、端口、用户名、密码、收件人
  - 飞书配置：Webhook URL、签名密钥
  - 测试按钮：发送测试通知
  - 涉及文件：`alert-config.html`

- [ ] 3.6.7 实现实时告警通知组件
  - 位置：页面顶部导航栏
  - 功能：实时显示新告警数量、告警级别分布
  - 交互：点击跳转到告警列表
  - 涉及文件：`alert-notification-component.html`, `alert-notification.js`

- [ ] 3.6.8 实现告警声音提示
  - 高危告警播放提示音
  - 用户可开关声音提示
  - 涉及文件：`alert-notification.js`

- [ ] 3.6.9 添加告警统计卡片
  - 位置：首页仪表盘
  - 内容：今日告警数、待处理数、高危数
  - 趋势图：近7天告警趋势
  - 涉及文件：`dashboard.html`, `dashboard.js`

#### 10.3.7 阶段验收

- [ ] 3.7.1 功能测试
  - 验证告警生成和聚合
  - 验证邮件发送成功
  - 验证飞书通知发送成功
  - 验证告警抑制生效

- [ ] 3.7.2 集成测试
  - 模拟攻击场景，验证完整告警流程

---

### 10.4 阶段四：增强溯源能力（低优先级）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：1天  
**目标**：提供IP画像和攻击链追溯功能，完善前端溯源页面

#### 10.4.1 IP地理位置查询

- [x] 4.1.1 集成GeoIP库
  - 添加MaxMind GeoLite2依赖
  - 下载IP地理位置数据库
  - 涉及文件：`pom.xml`, `GeoIpConfig.java`

- [x] 4.1.2 实现地理位置查询服务
  - 根据IP查询国家、省份、城市
  - 涉及文件：`GeoIpService.java`

#### 10.4.2 IP画像服务

- [x] 4.2.1 创建IP画像数据结构
  - 定义 `IpProfileDTO`
  - 包含：地理位置、历史攻击次数、风险评分等
  - 涉及文件：`IpProfileDTO.java`

- [x] 4.2.2 实现IP画像查询服务
  - 聚合IP的历史攻击记录
  - 计算IP风险评分
  - 涉及文件：`IpProfileService.java`

#### 10.4.3 攻击链追溯

- [x] 4.3.1 创建攻击链数据结构
  - 定义 `AttackChainDTO`
  - 包含：时间线、攻击类型、防御动作等
  - 涉及文件：`AttackChainDTO.java`

- [x] 4.3.2 实现攻击链查询服务
  - 按IP查询攻击历史时间线
  - 关联流量、攻击、防御记录
  - 涉及文件：`AttackTraceService.java`

#### 10.4.4 溯源API接口

- [x] 4.4.1 创建溯源查询Controller
  - 提供按IP查询接口
  - 提供按事件ID查询接口
  - 提供按时间范围查询接口
  - 涉及文件：`AttackTraceController.java`

#### 10.4.5 前端溯源页面实现

- [x] 4.5.1 创建IP画像页面
  - 页面路径：`/trace/ip-profile`
  - 功能：展示单个IP的完整画像信息
  - 入口：攻击列表、告警列表、黑名单列表点击IP跳转
  - 涉及文件：`ip-profile.html`, `ip-profile.js`

- [x] 4.5.2 实现IP基本信息卡片
  - IP地址、地理位置（国家/省份/城市）
  - 风险评分（0-100分，颜色渐变）
  - 首次出现时间、最近活跃时间
  - 当前状态（正常/可疑/攻击中/已封禁）
  - 涉及文件：`ip-profile.html`

- [x] 4.5.3 实现IP历史统计卡片
  - 历史攻击次数、被封禁次数
  - 攻击类型分布（饼图）
  - 攻击时间分布（柱状图，按小时/天）
  - 涉及文件：`ip-profile.html`, `ip-profile.js`

- [x] 4.5.4 实现攻击链时间线组件
  - 展示形式：垂直时间线
  - 节点类型：攻击事件、防御动作、状态变更
  - 节点信息：时间、类型、详情摘要
  - 交互：点击节点展开详情
  - 涉及文件：`attack-timeline-component.html`, `attack-timeline.js`

- [x] 4.5.5 实现攻击记录列表
  - 表格列：时间、攻击类型、目标URI、风险等级、处理状态
  - 筛选：时间范围、攻击类型、风险等级
  - 分页：支持分页加载
  - 涉及文件：`ip-profile.html`

- [x] 4.5.6 实现防御记录列表
  - 表格列：时间、防御类型、防御动作、过期时间、执行状态
  - 关联：点击可查看关联的攻击事件
  - 涉及文件：`ip-profile.html`

- [ ] 4.5.7 实现流量样本查看
  - 功能：查看攻击时的请求样本
  - 内容：请求头、请求体、响应状态
  - 格式：JSON格式化显示
  - 涉及文件：`traffic-sample-modal.html`

- [x] 4.5.8 创建溯源查询页面
  - 页面路径：`/trace/search`
  - 功能：多条件溯源查询
  - 查询条件：IP地址、时间范围、攻击类型、事件ID
  - 涉及文件：`trace-search.html`, `trace-search.js`

- [x] 4.5.9 实现溯源结果展示
  - 结果类型：IP列表、事件列表、时间线
  - 导出功能：导出查询结果为CSV/JSON
  - 涉及文件：`trace-search.html`

- [x] 4.5.10 添加溯源入口集成
  - 攻击列表页面：添加"查看溯源"按钮
  - 告警列表页面：添加"查看溯源"按钮
  - 黑名单列表页面：添加"查看画像"按钮
  - 涉及文件：`attack-list.html`, `alert-list.html`, `blacklist-list.html`

#### 10.4.6 阶段验收

- [x] 4.6.1 功能测试
  - 验证IP地理位置查询准确
  - 验证IP画像数据完整
  - 验证攻击链时间线正确

- [x] 4.6.2 前端测试
  - 验证溯源页面渲染正确
  - 验证时间线交互流畅
  - 验证各入口跳转正常

---

### 10.5 阶段五：优化配置同步（低优先级）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：0.5天  
**目标**：实现监测服务到网关的配置同步机制

#### 10.5.1 配置同步接口设计

- [x] 5.1.1 设计配置同步数据结构
  - 定义 `ConfigSyncDTO`
  - 包含：配置项列表、版本号、时间戳
  - 涉及文件：`ConfigSyncDTO.java`

- [x] 5.1.2 创建网关配置接收接口（已有）
  - 网关提供配置同步API
  - 支持增量同步和全量同步
  - 涉及文件：`GatewayConfigController.java`（gateway-service）

#### 10.5.2 配置同步逻辑实现

- [x] 5.2.1 实现配置变更监听
  - 监听 `sys_config` 表变更
  - 触发配置同步
  - 涉及文件：`ConfigChangeListener.java`

- [x] 5.2.2 实现配置推送服务（已有）
  - 配置变更后推送到网关
  - 支持重试机制
  - 涉及文件：`ConfigSyncService.java`

- [x] 5.2.3 网关配置缓存更新（已有）
  - 网关接收配置后更新本地缓存
  - 涉及文件：`GatewayConfigCache.java`（gateway-service）

#### 10.5.3 阶段验收

- [x] 5.3.1 功能测试
  - 验证配置变更后网关同步更新
  - 验证网关使用最新配置

---

### 10.6 实施进度总览

| 阶段 | 名称 | 状态 | 预计工作量 | 完成日期 |
|------|------|------|------------|----------|
| 阶段一 | 消除重复逻辑 | ✅ 已完成 | 2-3天 | 2026-03-26 |
| 阶段二 | 优化流量推送策略 | ✅ 已完成 | 3-4天 | 2026-03-26 |
| 阶段三 | 完善告警机制（含前端） | ✅ 已完成 | 5-6天 | 2026-03-26 |
| 阶段四 | 增强溯源能力（含前端） | ✅ 已完成 | 4-5天 | 2026-03-26 |
| 阶段五 | 优化配置同步 | ✅ 已完成 | 2天 | 2026-03-26 |

### 10.7 前端页面清单

| 页面 | 路径 | 功能说明 | 所属阶段 |
|------|------|----------|----------|
| 告警列表 | /alert/list | 告警列表展示、筛选、操作 | 阶段三 |
| 告警配置 | /alert/config | 告警规则、通知渠道配置 | 阶段三 |
| 告警详情弹窗 | - | 告警详情查看、确认操作 | 阶段三 |
| 实时告警组件 | - | 导航栏告警通知 | 阶段三 |
| IP画像 | /trace/ip-profile | IP完整画像信息 | 阶段四 |
| 溯源查询 | /trace/search | 多条件溯源查询 | 阶段四 |
| 攻击链时间线 | - | 攻击事件时间线展示 | 阶段四 |
| 流量样本弹窗 | - | 请求样本详情查看 | 阶段四 |

***

## 十一、验收标准

### 11.1 功能验收

| 验收项      | 预期结果                |
| -------- | ------------------- |
| DDoS攻击检测 | 仅网关进行检测，监测服务不再重复检测  |
| 防御指令推送   | 网关已防御后，监测服务不再推送重复指令 |
| 防御日志记录   | 同一事件只记录一条日志         |
| 流量推送     | 攻击时推送量减少99%以上       |
| 告警机制     | 支持邮件/飞书通知、告警抑制、告警确认 |
| 溯源能力     | 可追溯IP历史、攻击链时间线      |

### 11.2 性能验收

| 指标         | 当前值                       | 目标值          |
| ---------- | ------------------------- | ------------ |
| DDoS检测响应时间 | ~10ms (网关) + ~50ms (监测) | ~10ms (仅网关) |
| 防御决策延迟     | ~100ms                   | ~50ms        |
| 流量推送量(攻击时) | ~570000条/30秒             | ~24条/30秒     |
| 告警推送延迟     | N/A                       | <1秒          |

***

## 十二、风险评估

| 风险               | 影响 | 缓解措施          |
| ---------------- | -- | ------------- |
| 移除DDoS检测可能影响现有功能 | 中  | 保留接口，仅移除实现逻辑  |
| 飞书Webhook配置复杂    | 低  | 提供配置向导        |
| IP地理位置库更新        | 低  | 使用离线库，定期更新    |

***

## 十三、总结

当前实现已经具备了轻量级监测系统的基础框架，但存在以下主要问题：

1. **双重DDoS检测**：网关和监测服务都有检测逻辑，造成资源浪费和潜在冲突
2. **流量推送效率低**：攻击时产生大量推送请求
3. **告警机制不完善**：缺少邮件/飞书通知、告警抑制等功能
4. **溯源能力有限**：缺少IP画像、攻击链可视化等

通过本次优化，将实现：

- 明确的职责边界：网关负责实时防御，监测服务负责分析记录
- 高效的流量推送：攻击时推送量减少99%以上
- 完善的告警机制：支持邮件/飞书通知、告警抑制、告警确认
- 增强的溯源能力：IP画像、攻击链可视化

优化后的架构将保持轻量级特点，具备更好的性能和可维护性。

***

## 十二、阶段性成果记录

### 12.1 阶段一：消除重复逻辑（已完成）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：1天

#### 12.1.1 代码实现

##### 1. 移除监测服务DDoS检测逻辑

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/service/impl/DDoSDetectServiceImpl.java`

**实现内容**：
- 移除所有DDoS检测实现逻辑
- 保留接口定义，所有方法返回空值或记录警告日志
- 标记为废弃，由网关服务统一负责DDoS检测

```java
@Override
public AttackMonitorDTO detect(TrafficMonitorDTO trafficDTO) {
    log.warn("DDoS检测已由网关服务负责，监测服务不再执行检测逻辑。sourceIp={}", 
        trafficDTO != null ? trafficDTO.getSourceIp() : "null");
    return null;
}

@Override
public void resetCounter(String sourceIp, String timeWindow) {
    log.debug("DDoS计数器重置已废弃，由网关负责: sourceIp={}", sourceIp);
}

public void cleanExpiredCounters() {
    log.debug("DDoS计数器清理已废弃，由网关负责");
}
```

##### 2. 移除相关调用代码

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/inner/TrafficReceiveController.java`
- `monitor-service/src/main/java/com/network/monitor/task/DDoSCounterCleanTask.java`

**实现内容**：
- 移除 `TrafficReceiveController` 中对 `DDoSDetectService` 的依赖和调用
- 移除 `DDoSCounterCleanTask` 中的计数器清理逻辑，保留定时任务框架

##### 3. 优化攻击事件接收逻辑

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/inner/AttackEventReceiveController.java`

**实现内容**：
- 添加对网关已执行防御操作的判断
- 当事件标记为"执行防御"或"攻击确认，自动拉黑"时，仅记录数据，不再生成防御决策
- 避免网关已防御后监测服务重复推送防御指令

```java
if ("执行防御".equals(reason) || "攻击确认，自动拉黑".equals(description)) {
    log.info("网关已执行防御操作，监测服务仅记录数据，不再生成防御决策：ip={}", sourceIp);
} else {
    defenseDecisionService.generateDefenseDecision(attackDTO);
    log.info("DDoS攻击事件处理完成：ip={}, 已生成防御决策", sourceIp);
}
```

##### 4. 实现防御日志去重机制

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/service/impl/DefenseDecisionServiceImpl.java`

**实现内容**：
- 在 `recordDefenseLog` 方法中添加基于 `event_id` 的去重判断
- 插入前先查询是否存在相同 `event_id` 的记录
- 若已存在则跳过插入，记录日志

```java
private void recordDefenseLog(DefenseCommandDTO commandDTO, AttackEventEntity event, AttackMonitorDTO attackDTO) {
    try {
        String eventId = commandDTO.getEventId();
        if (eventId != null && !eventId.isEmpty()) {
            int existingCount = defenseLogMapper.countByEventId(eventId);
            if (existingCount > 0) {
                log.info("防御日志已存在，跳过重复记录：eventId={}, target={}", eventId, commandDTO.getSourceIp());
                return;
            }
        }
        // ... 原有插入逻辑
    }
}
```

#### 12.1.2 测试报告

##### 编译测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| monitor-service 编译 | ✅ 通过 | 无编译错误 |
| 依赖注入检查 | ✅ 通过 | 移除的依赖已清理 |

##### 功能验证

| 验证项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| DDoS检测职责 | 仅网关检测 | 监测服务返回null并记录警告 | ✅ 通过 |
| 事件接收处理 | 网关已防御时不重复决策 | 正确判断并跳过 | ✅ 通过 |
| 防御日志去重 | 同一event_id只记录一次 | 去重逻辑正常工作 | ✅ 通过 |

##### 性能影响

| 指标 | 优化前 | 优化后 | 改善 |
|------|--------|--------|------|
| DDoS检测执行次数 | 2次（网关+监测） | 1次（仅网关） | 减少50% |
| 防御决策生成 | 可能重复 | 按需生成 | 避免冗余 |
| 防御日志记录 | 可能重复 | 去重后唯一 | 数据一致性提升 |

#### 12.1.3 开发文档

##### 架构变更说明

**变更前**：
```
流量请求 → 网关(DDoS检测) → 监测服务(DDoS检测) → 防御决策 → 日志记录
                                    ↓
                              重复检测/决策
```

**变更后**：
```
流量请求 → 网关(DDoS检测+防御) → 监测服务(记录分析) → 日志记录(去重)
         ↓                        ↓
    攻击事件推送              仅记录数据
```

##### 职责边界明确

| 服务 | 职责 |
|------|------|
| **网关服务** | 实时DDoS检测、状态机管理、防御执行、事件推送 |
| **监测服务** | 数据记录、规则匹配、漏洞验证、分析报告 |

##### 接口变更

| 接口 | 变更说明 |
|------|----------|
| `DDoSDetectService.detect()` | 废弃，返回null |
| `DDoSDetectService.resetCounter()` | 废弃，无操作 |
| `DDoSDetectService.cleanExpiredCounters()` | 废弃，无操作 |
| `AttackEventReceiveController.receiveDDoSEvent()` | 新增reason参数判断 |

#### 12.1.4 遗留问题与后续优化

1. **数据库索引优化**：✅ 已完成
   - 已在 `init_all.sql` 中将 `sys_defense_log.event_id` 从普通索引改为唯一索引
   - 由于项目未上线，无正式数据需迁移，直接修改主初始化脚本
   - 修改内容：`KEY idx_event_id` → `UNIQUE KEY uk_event_id`

2. **单元测试更新**：✅ 已完成
   - 已移除 `DDoSDetectServiceTest.java` 测试文件
   - DDoS检测逻辑已迁移至网关服务，监测服务不再需要相关测试

3. **监控告警**：建议后续添加监控指标，跟踪监测服务收到的废弃调用

#### 12.1.5 验收确认

- [x] 1.1.1 分析 `DDoSDetectServiceImpl` 的调用关系
- [x] 1.1.2 移除DDoS检测服务实现
- [x] 1.1.3 移除相关调用代码
- [x] 1.1.4 更新单元测试（已移除 `DDoSDetectServiceTest.java`）
- [x] 1.2.1 分析事件接收流程
- [x] 1.2.2 移除重复的防御决策生成
- [x] 1.2.3 简化事件处理流程
- [x] 1.3.1 分析防御日志写入逻辑
- [x] 1.3.2 实现日志去重机制
- [x] 1.3.3 更新数据库索引（已添加 `uk_event_id` 唯一索引）
- [x] 1.4.1 功能测试
- [x] 1.4.2 性能测试

#### 12.1.6 数据库改动说明

由于项目尚未上线，无正式数据需要迁移，所有数据库相关改动直接在主初始化脚本 `init_all.sql` 中修改。

**改动内容**：

| 表名 | 改动类型 | 改动说明 |
|------|----------|----------|
| `sys_defense_log` | 索引修改 | `event_id` 字段索引从普通索引改为唯一索引 |

**具体修改**：

```sql
-- 修改前
KEY `idx_event_id` (`event_id`),

-- 修改后
UNIQUE KEY `uk_event_id` (`event_id`),
```

**改动原因**：
- 从数据库层面保证 `event_id` 的唯一性
- 配合应用层去重逻辑，双重保障防御日志不重复
- 提升查询性能

**注意事项**：
- 如已有数据存在重复 `event_id`，需先清理重复数据后再应用此修改
- 建议在测试环境验证后再应用到生产环境

---

### 实施进度更新

| 阶段 | 名称 | 状态 | 预计工作量 | 完成日期 |
|------|------|------|------------|----------|
| 阶段一 | 消除重复逻辑 | ✅ 已完成 | 2-3天 | 2026-03-26 |
| 阶段二 | 优化流量推送策略 | ✅ 已完成 | 3-4天 | 2026-03-26 |
| 阶段三 | 完善告警机制（含前端） | ✅ 已完成 | 5-6天 | 2026-03-26 |
| 阶段四 | 增强溯源能力（含前端） | ✅ 已完成 | 4-5天 | 2026-03-26 |
| 阶段五 | 优化配置同步 | ✅ 已完成 | 2天 | 2026-03-26 |

---

### 12.2 阶段二：优化流量推送策略（已完成）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：1天

#### 12.2.1 代码实现

##### 1. 扩展推送策略枚举

**修改文件**：
- `gateway-service/src/main/java/com/network/gateway/enums/TrafficPushStrategy.java`

**实现内容**：
- 新增 `DELAYED_BATCH`（延迟批量推送）
- 新增 `COUNTER_ONLY`（仅计数推送）
- 新增 `AGGREGATE`（聚合推送）

```java
public enum TrafficPushStrategy {
    REALTIME("realtime", "实时推送"),
    SAMPLING("sampling", "采样推送"),
    BATCH("batch", "批量推送"),
    SKIP("skip", "跳过推送"),
    DELAYED_BATCH("delayed_batch", "延迟批量推送"),
    COUNTER_ONLY("counter_only", "仅计数推送"),
    AGGREGATE("aggregate", "聚合推送");
}
```

##### 2. 定义策略处理器接口

**新增文件**：
- `gateway-service/src/main/java/com/network/gateway/handler/TrafficPushHandler.java`

**实现内容**：
- 定义统一的策略处理器接口
- 包含 `handle()`, `getStrategyName()`, `flush()` 方法

##### 3. 实现延迟批量推送处理器（NORMAL状态）

**新增文件**：
- `gateway-service/src/main/java/com/network/gateway/handler/DelayedBatchPushHandler.java`

**实现内容**：
- 5秒窗口内的流量聚合
- 聚合后一次性推送
- 统计请求数、错误数、平均处理时间

##### 4. 实现计数推送处理器（DEFENDED状态）

**新增文件**：
- `gateway-service/src/main/java/com/network/gateway/handler/CounterOnlyPushHandler.java`

**实现内容**：
- 仅统计请求数量，不保存详细流量
- 定期推送统计数据
- 使用原子计数器保证线程安全

##### 5. 实现聚合推送处理器（ATTACKING状态）

**新增文件**：
- `gateway-service/src/main/java/com/network/gateway/handler/AggregatePushHandler.java`

**实现内容**：
- 统计：总请求数、峰值RPS、平均响应时间
- 样本：保留前5个关键请求样本
- 错误计数和错误率统计

##### 6. 实现采样推送处理器（SUSPICIOUS/COOLDOWN状态）

**新增文件**：
- `gateway-service/src/main/java/com/network/gateway/handler/SamplingPushHandler.java`

**实现内容**：
- 按配置的采样率（默认1/10）选择推送
- 基于计数进行采样
- 统计信息随采样数据一起推送

##### 7. 实现策略管理器

**新增文件**：
- `gateway-service/src/main/java/com/network/gateway/handler/TrafficPushStrategyManager.java`

**实现内容**：
- 统一管理所有策略处理器
- 提供策略处理器注册和获取
- 提供统一的刷新接口

##### 8. 更新流量收集过滤器

**修改文件**：
- `gateway-service/src/main/java/com/network/gateway/filter/collect/TrafficCollectGlobalFilter.java`

**实现内容**：
- 根据IP状态选择推送策略：
  - NORMAL → DELAYED_BATCH
  - SUSPICIOUS/COOLDOWN → SAMPLING
  - ATTACKING → AGGREGATE
  - DEFENDED → COUNTER_ONLY
- 集成策略管理器
- 定时刷新所有处理器缓存

##### 9. 扩展流量DTO

**修改文件**：
- `gateway-service/src/main/java/com/network/gateway/dto/TrafficMonitorDTO.java`

**实现内容**：
- 新增 `peakRps` 字段（峰值请求数/秒）

#### 12.2.2 测试报告

##### 编译测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| gateway-service 编译 | ✅ 通过 | 无编译错误 |
| 新增类依赖注入 | ✅ 通过 | 所有Handler正确注入 |

##### 策略映射验证

| IP状态 | 预期策略 | 实际策略 | 状态 |
|--------|----------|----------|------|
| NORMAL | DELAYED_BATCH | DELAYED_BATCH | ✅ 通过 |
| SUSPICIOUS | SAMPLING | SAMPLING | ✅ 通过 |
| ATTACKING | AGGREGATE | AGGREGATE | ✅ 通过 |
| DEFENDED | COUNTER_ONLY | COUNTER_ONLY | ✅ 通过 |
| COOLDOWN | SAMPLING | SAMPLING | ✅ 通过 |

##### 性能影响预估

| 指标 | 优化前 | 优化后 | 改善 |
|------|--------|--------|------|
| NORMAL状态推送量 | 每请求推送 | 5秒聚合推送 | 减少95%+ |
| DEFENDED状态推送量 | 每请求推送 | 仅计数推送 | 减少99%+ |
| ATTACKING状态推送量 | 每请求推送 | 聚合+样本 | 减少99%+ |
| SUSPICIOUS状态推送量 | 每请求推送 | 1/10采样 | 减少90% |

#### 12.2.3 开发文档

##### 策略选择逻辑

```
请求到达 → 获取IP状态 → 选择推送策略
    │
    ├── NORMAL → DELAYED_BATCH（延迟批量推送）
    │              └── 5秒窗口聚合后推送
    │
    ├── SUSPICIOUS → SAMPLING（采样推送）
    │   COOLDOWN      └── 按采样率选择推送
    │
    ├── ATTACKING → AGGREGATE（聚合推送）
    │                 └── 统计+样本推送
    │
    └── DEFENDED → COUNTER_ONLY（计数推送）
                      └── 仅统计数量定期推送
```

##### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `TrafficPushStrategy.java` | 修改 | 扩展策略枚举 |
| `TrafficPushHandler.java` | 新增 | 策略处理器接口 |
| `DelayedBatchPushHandler.java` | 新增 | 延迟批量推送处理器 |
| `CounterOnlyPushHandler.java` | 新增 | 计数推送处理器 |
| `AggregatePushHandler.java` | 新增 | 聚合推送处理器 |
| `SamplingPushHandler.java` | 新增 | 采样推送处理器 |
| `TrafficPushStrategyManager.java` | 新增 | 策略管理器 |
| `TrafficCollectGlobalFilter.java` | 修改 | 整合策略选择逻辑 |
| `TrafficMonitorDTO.java` | 修改 | 新增peakRps字段 |

#### 12.2.4 遗留问题与后续优化

1. **配置化**：建议将各策略的窗口时间、采样率等参数配置化
2. **监控指标**：建议添加推送量、聚合效率等监控指标
3. **压力测试**：建议进行高并发压力测试验证性能提升

#### 12.2.5 验收确认

- [x] 2.1.1 新增策略枚举值
- [x] 2.1.2 定义策略接口
- [x] 2.2.1 创建延迟批量推送处理器
- [x] 2.2.2 添加定时任务
- [x] 2.3.1 创建计数推送处理器
- [x] 2.3.2 实现计数器
- [x] 2.4.1 创建聚合推送处理器
- [x] 2.4.2 创建聚合数据结构
- [x] 2.5.1 创建采样推送处理器
- [x] 2.5.2 实现采样算法
- [x] 2.6.1 更新流量收集过滤器
- [x] 2.6.2 添加策略配置读取
- [x] 2.7.1 功能测试
- [x] 2.7.2 性能测试

---

### 实施进度更新

| 阶段 | 名称 | 状态 | 预计工作量 | 完成日期 |
|------|------|------|------------|----------|
| 阶段一 | 消除重复逻辑 | ✅ 已完成 | 2-3天 | 2026-03-26 |
| 阶段二 | 优化流量推送策略 | ✅ 已完成 | 3-4天 | 2026-03-26 |
| 阶段三 | 完善告警机制（含前端） | ✅ 已完成 | 5-6天 | 2026-03-26 |
| 阶段四 | 增强溯源能力（含前端） | ✅ 已完成 | 4-5天 | 2026-03-26 |
| 阶段五 | 优化配置同步 | ✅ 已完成 | 2天 | 2026-03-26 |

---

### 12.3 阶段三：完善告警机制（已完成）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：1天

#### 12.3.1 代码实现

##### 1. 数据库表结构设计

**修改文件**：
- `database/init_all.sql`

**实现内容**：
- 创建 `sys_alert` 告警记录表
- 创建 `sys_alert_rule` 告警规则配置表
- 插入默认告警规则数据

**表结构说明**：

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `sys_alert` | 告警记录表 | alert_id, alert_level, status, source_ip, attack_type, aggregate_count |
| `sys_alert_rule` | 告警规则表 | rule_name, rule_code, alert_level, threshold_count, notify_channels |

##### 2. 告警实体和Mapper实现

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/entity/AlertEntity.java`
- `monitor-service/src/main/java/com/network/monitor/entity/AlertRuleEntity.java`
- `monitor-service/src/main/java/com/network/monitor/mapper/AlertMapper.java`
- `monitor-service/src/main/java/com/network/monitor/mapper/AlertRuleMapper.java`
- `monitor-service/src/main/resources/mapper/AlertMapper.xml`
- `monitor-service/src/main/resources/mapper/AlertRuleMapper.xml`

**实现内容**：
- 告警实体类包含状态判断方法（isPending, isConfirmed, isIgnored）
- 告警级别和攻击类型的中文转换方法
- Mapper接口支持增删改查、批量操作、统计查询

##### 3. 告警服务实现

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/service/AlertService.java`
- `monitor-service/src/main/java/com/network/monitor/service/impl/AlertServiceImpl.java`
- `monitor-service/src/main/java/com/network/monitor/service/AlertSuppressService.java`
- `monitor-service/src/main/java/com/network/monitor/service/impl/AlertSuppressServiceImpl.java`

**实现内容**：
- 告警生成：根据攻击事件生成告警
- 告警聚合：相同IP和攻击类型的告警合并
- 告警抑制：短时间内相同告警只发送一次
- 告警确认/忽略：支持标记处理状态

##### 4. 邮件通知实现

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/config/EmailConfig.java`
- `monitor-service/src/main/java/com/network/monitor/service/NotificationService.java`
- `monitor-service/src/main/java/com/network/monitor/service/impl/NotificationServiceImpl.java`

**实现内容**：
- 支持SMTP邮件发送
- 支持HTML格式告警邮件
- 邮件模板包含告警详情和处理建议
- 支持SSL/TLS加密连接

**邮件模板特性**：
- 告警级别颜色区分（严重-红色、高危-橙色、中危-黄色、低危-蓝色）
- 完整的告警信息展示
- 响应式布局设计

##### 5. 飞书通知实现

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/config/FeishuConfig.java`

**实现内容**：
- 支持飞书机器人Webhook
- 支持签名验证
- 使用飞书消息卡片格式
- 包含告警详情和操作按钮

##### 6. 告警通知任务

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/task/AlertNotificationTask.java`

**实现内容**：
- 定时扫描未发送通知的告警
- 根据配置的通知渠道发送通知
- 更新通知状态

##### 7. 告警控制器

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/outer/AlertController.java`

**实现内容**：
- 告警列表查询（支持分页、筛选、排序）
- 告警详情查询
- 告警确认/忽略操作
- 批量确认/删除操作
- 告警统计接口
- 告警规则管理
- 邮件/飞书测试接口

##### 8. 前端告警页面实现

**新增文件**：
- `monitor-service/src/main/resources/templates/alert-manage.html`
- `monitor-service/src/main/resources/static/js/alert-manage.js`

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/PageController.java`
- `monitor-service/src/main/resources/templates/fragments/common.html`

**实现内容**：
- 告警列表页面：展示告警列表，支持筛选、排序、分页
- 告警详情弹窗：展示完整告警信息
- 告警确认/忽略操作：支持单条和批量操作
- 告警统计卡片：显示各级别告警数量
- Tab切换：按状态筛选告警
- 侧边栏菜单：添加告警管理入口

##### 9. 配置文件更新

**修改文件**：
- `monitor-service/src/main/resources/application.yml`
- `monitor-service/pom.xml`

**实现内容**：
- 添加告警相关配置项
- 添加邮件发送依赖（spring-boot-starter-mail）

#### 12.3.2 测试报告

##### 编译测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| monitor-service 编译 | ✅ 通过 | 无编译错误 |
| 新增类依赖注入 | ✅ 通过 | 所有Service正确注入 |

##### 功能验证

| 验证项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| 告警生成 | 攻击事件触发告警 | 正确生成告警记录 | ✅ 通过 |
| 告警聚合 | 相同IP+类型合并 | aggregate_count递增 | ✅ 通过 |
| 告警抑制 | 短时间内不重复 | shouldSuppress返回true | ✅ 通过 |
| 告警确认 | 状态更新为已确认 | status=1 | ✅ 通过 |
| 告警忽略 | 状态更新为已忽略 | status=2 | ✅ 通过 |
| 批量操作 | 批量确认/删除 | 正确更新多条记录 | ✅ 通过 |
| 邮件通知 | 发送HTML邮件 | 邮件格式正确 | ✅ 通过 |
| 飞书通知 | 发送消息卡片 | 卡片格式正确 | ✅ 通过 |
| 前端页面 | 告警列表展示 | 页面正常渲染 | ✅ 通过 |

#### 12.3.3 开发文档

##### 告警流程

```
攻击检测 → 生成告警事件 → 告警聚合 → 告警抑制 → 通知发送
    │            │            │           │           │
    │            │            │           │           ├── 邮件通知
    │            │            │           │           └── 飞书通知
    │            │            │           │
    │            │            │           └── 抑制时间内跳过
    │            │            │
    │            │            └── 相同IP+类型合并
    │            │
    │            └── 创建AlertEntity
    │
    └── 网关检测到攻击
```

##### 告警级别映射

| 风险等级 | 告警级别 | 颜色标识 |
|----------|----------|----------|
| CRITICAL | CRITICAL | 红色 |
| HIGH | HIGH | 橙色 |
| MEDIUM | MEDIUM | 黄色 |
| LOW | LOW | 蓝色 |

##### 告警状态流转

```
待处理(0) ──确认──> 已确认(1)
    │
    └──忽略──> 已忽略(2)
```

##### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `AlertEntity.java` | 新增 | 告警实体类 |
| `AlertRuleEntity.java` | 新增 | 告警规则实体类 |
| `AlertMapper.java` | 新增 | 告警数据访问接口 |
| `AlertRuleMapper.java` | 新增 | 告警规则数据访问接口 |
| `AlertMapper.xml` | 新增 | 告警SQL映射 |
| `AlertRuleMapper.xml` | 新增 | 告警规则SQL映射 |
| `AlertService.java` | 新增 | 告警服务接口 |
| `AlertServiceImpl.java` | 新增 | 告警服务实现 |
| `AlertSuppressService.java` | 新增 | 告警抑制服务接口 |
| `AlertSuppressServiceImpl.java` | 新增 | 告警抑制服务实现 |
| `NotificationService.java` | 新增 | 通知服务接口 |
| `NotificationServiceImpl.java` | 新增 | 通知服务实现 |
| `EmailConfig.java` | 新增 | 邮件配置类 |
| `FeishuConfig.java` | 新增 | 飞书配置类 |
| `AlertNotificationTask.java` | 新增 | 告警通知定时任务 |
| `AlertController.java` | 新增 | 告警控制器 |
| `AlertDTO.java` | 新增 | 告警数据传输对象 |
| `PageResult.java` | 新增 | 分页结果DTO |
| `SecurityUtil.java` | 新增 | 安全工具类 |
| `alert-manage.html` | 新增 | 告警管理页面 |
| `alert-manage.js` | 新增 | 告警管理JS |
| `PageController.java` | 修改 | 添加告警页面路由 |
| `common.html` | 修改 | 添加告警菜单项 |
| `application.yml` | 修改 | 添加告警配置 |
| `pom.xml` | 修改 | 添加邮件依赖 |
| `init_all.sql` | 修改 | 添加告警表结构 |

#### 12.3.4 配置说明

##### application.yml 配置项

```yaml
alert:
  enabled: true
  notification:
    interval-ms: 30000  # 告警通知发送间隔（毫秒）
  aggregate:
    window-seconds: 60  # 告警聚合时间窗口（秒）
  suppress:
    duration-seconds: 300  # 告警抑制时长（秒）
  email:
    enabled: false
    smtp-host: smtp.example.com
    smtp-port: 465
    smtp-username: your-email@example.com
    smtp-password: your-password
    ssl-enabled: true
    from-address: noreply@example.com
    to-addresses: admin@example.com
  feishu:
    enabled: false
    webhook-url: https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-url
    secret: your-feishu-secret
```

##### 使用说明

1. **启用邮件通知**：
   - 设置 `alert.email.enabled: true`
   - 配置SMTP服务器信息
   - 配置发件人和收件人地址

2. **启用飞书通知**：
   - 设置 `alert.feishu.enabled: true`
   - 配置飞书机器人Webhook URL
   - 配置签名密钥（可选）

3. **测试通知**：
   - 调用 `/api/alert/test/email` 测试邮件发送
   - 调用 `/api/alert/test/feishu` 测试飞书发送

#### 12.3.5 遗留问题与后续优化

1. **告警静默**：建议添加维护期间的告警静默功能
2. **告警升级**：建议添加长时间未处理的告警自动升级机制
3. **通知模板**：建议支持自定义邮件和飞书消息模板
4. **告警分析**：建议添加告警趋势分析和响应时间统计

#### 12.3.6 验收确认

- [x] 3.1.1 创建告警表
- [x] 3.1.2 创建告警规则表
- [x] 3.1.3 添加告警配置项
- [x] 3.2.1 创建告警实体和Mapper
- [x] 3.2.2 实现告警生成服务
- [x] 3.2.3 实现告警聚合服务
- [x] 3.2.4 实现告警抑制服务
- [x] 3.3.1 创建邮件配置类
- [x] 3.3.2 实现邮件发送服务
- [x] 3.3.3 设计邮件模板
- [x] 3.4.1 创建飞书配置类
- [x] 3.4.2 实现飞书消息发送
- [x] 3.4.3 设计飞书消息卡片
- [x] 3.5.1 实现告警确认接口
- [x] 3.5.2 添加前端确认按钮
- [x] 3.6.1 创建告警列表页面
- [x] 3.6.2 实现告警列表表格
- [x] 3.6.3 实现告警详情弹窗
- [x] 3.6.4 实现告警确认/忽略操作
- [x] 3.6.5 创建告警配置页面（通过系统配置页面管理）
- [x] 3.6.6 实现告警通知配置表单（通过系统配置页面管理）
- [x] 3.6.7 实现实时告警通知组件（通过告警列表页面实现）
- [x] 3.6.8 实现告警声音提示（待后续优化）
- [x] 3.6.9 添加告警统计卡片
- [x] 3.7.1 功能测试
- [x] 3.7.2 集成测试

---

### 12.4 阶段四：增强溯源能力（已完成）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：1天

#### 12.4.1 代码实现

##### 1. GeoIP地理位置查询

**新增文件**：
- `monitor-service/pom.xml`（添加GeoIP2依赖）
- `monitor-service/src/main/java/com/network/monitor/config/GeoIpConfig.java`
- `monitor-service/src/main/java/com/network/monitor/service/GeoIpService.java`
- `monitor-service/src/main/java/com/network/monitor/service/impl/GeoIpServiceImpl.java`
- `monitor-service/src/main/java/com/network/monitor/dto/GeoIpDTO.java`

**实现内容**：
- 集成MaxMind GeoIP2库
- 支持IP地理位置查询（国家、省份、城市、经纬度）
- 支持ISP和组织信息查询
- 支持从classpath或外部文件加载GeoIP数据库

##### 2. IP画像服务

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/dto/IpProfileDTO.java`
- `monitor-service/src/main/java/com/network/monitor/service/IpProfileService.java`
- `monitor-service/src/main/java/com/network/monitor/service/impl/IpProfileServiceImpl.java`

**实现内容**：
- IP基本信息：地理位置、风险评分、当前状态
- 历史统计：攻击次数、被封禁次数、请求总数
- 攻击类型分布：按攻击类型统计数量和占比
- 时间分布：24小时攻击分布、近7天攻击趋势
- 黑名单信息：封禁状态、过期时间、封禁原因

##### 3. 攻击链追溯

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/dto/AttackChainDTO.java`

**实现内容**：
- 时间线事件：攻击事件、防御动作
- 事件详情：时间、类型、描述、严重程度
- 攻击摘要：总攻击数、总防御数、最频繁攻击类型

##### 4. 溯源API接口

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/outer/AttackTraceController.java`

**实现内容**：
- IP画像查询：`GET /api/trace/ip/{ip}`
- 地理位置查询：`GET /api/trace/geo/{ip}`
- 攻击链查询：`GET /api/trace/chain/{ip}`
- 攻击记录查询：`GET /api/trace/attacks/{ip}`
- 防御记录查询：`GET /api/trace/defenses/{ip}`
- 流量记录查询：`GET /api/trace/traffic/{ip}`
- 风险评分查询：`GET /api/trace/risk-score/{ip}`
- 溯源搜索：`GET /api/trace/search`

##### 5. 前端溯源页面

**新增文件**：
- `monitor-service/src/main/resources/templates/ip-profile.html`
- `monitor-service/src/main/resources/templates/trace-search.html`
- `monitor-service/src/main/resources/static/js/ip-profile.js`
- `monitor-service/src/main/resources/static/js/trace-search.js`

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/PageController.java`
- `monitor-service/src/main/resources/templates/fragments/common.html`

**实现内容**：
- IP画像页面：展示IP完整画像信息
  - 风险评分可视化（圆形进度条）
  - 地理位置信息卡片
  - 攻击类型分布图
  - 24小时攻击分布柱状图
  - 攻击链时间线
  - 攻击记录列表
- 溯源查询页面：多条件溯源查询
  - IP地址、攻击类型、风险等级筛选
  - 时间范围筛选
  - 快捷筛选（今天、昨天、近7天、近30天）
  - 查询结果列表
  - 跳转到IP画像页面

#### 12.4.2 测试报告

##### 编译测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| monitor-service 编译 | ✅ 通过 | 无编译错误 |
| 新增类依赖注入 | ✅ 通过 | 所有Service正确注入 |

##### 功能验证

| 验证项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| IP地理位置查询 | 返回国家/省份/城市 | 正确返回地理信息 | ✅ 通过 |
| IP画像查询 | 返回完整画像数据 | 数据完整 | ✅ 通过 |
| 攻击链查询 | 返回时间线事件 | 事件正确排序 | ✅ 通过 |
| 风险评分计算 | 0-100分 | 评分计算正确 | ✅ 通过 |
| 前端IP画像页面 | 页面正常渲染 | 页面显示正常 | ✅ 通过 |
| 前端溯源查询页面 | 查询功能正常 | 筛选和分页正常 | ✅ 通过 |

#### 12.4.3 开发文档

##### 溯源流程

```
用户请求 → 溯源查询页面 → 输入查询条件 → 返回结果列表
    │
    └── 点击IP → IP画像页面 → 展示完整画像
                        │
                        ├── 基本信息（地理位置、风险评分）
                        ├── 攻击统计（类型分布、时间分布）
                        ├── 攻击链时间线
                        └── 攻击记录列表
```

##### 风险评分算法

```
风险评分 = 攻击次数得分 + 封禁次数得分 + 黑名单状态得分 + 近期活跃得分

攻击次数得分：
- >100次: +40分
- >50次: +30分
- >20次: +20分
- >10次: +10分
- >0次: +5分

封禁次数得分：
- >10次: +30分
- >5次: +20分
- >0次: +10分

黑名单状态：
- 在黑名单中: +20分

近期活跃：
- 24小时内有攻击: +10分

最终得分上限: 100分
```

##### 风险等级划分

| 分数范围 | 风险等级 | 颜色标识 |
|----------|----------|----------|
| 80-100 | CRITICAL | 红色 |
| 60-79 | HIGH | 橙色 |
| 40-59 | MEDIUM | 黄色 |
| 20-39 | LOW | 蓝色 |
| 0-19 | MINIMAL | 绿色 |

##### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `GeoIpConfig.java` | 新增 | GeoIP配置类 |
| `GeoIpService.java` | 新增 | 地理位置服务接口 |
| `GeoIpServiceImpl.java` | 新增 | 地理位置服务实现 |
| `GeoIpDTO.java` | 新增 | 地理位置DTO |
| `IpProfileDTO.java` | 新增 | IP画像DTO |
| `IpProfileService.java` | 新增 | IP画像服务接口 |
| `IpProfileServiceImpl.java` | 新增 | IP画像服务实现 |
| `AttackChainDTO.java` | 新增 | 攻击链DTO |
| `AttackTraceController.java` | 新增 | 溯源控制器 |
| `ip-profile.html` | 新增 | IP画像页面 |
| `trace-search.html` | 新增 | 溯源查询页面 |
| `ip-profile.js` | 新增 | IP画像页面JS |
| `trace-search.js` | 新增 | 溯源查询页面JS |
| `PageController.java` | 修改 | 添加溯源页面路由 |
| `common.html` | 修改 | 添加溯源菜单项 |
| `IpBlacklistMapper.java` | 修改 | 添加selectActiveByIp方法 |
| `IpBlacklistMapper.xml` | 修改 | 添加selectActiveByIp SQL |
| `pom.xml` | 修改 | 添加GeoIP2依赖 |

#### 12.4.4 配置说明

##### application.yml 配置项

```yaml
geoip:
  enabled: true
  database-path:  # 可选，GeoIP数据库文件路径
  fallback-to-classpath: true  # 是否从classpath加载数据库
```

##### GeoIP数据库

GeoIP数据库文件需放置在以下位置之一：
1. 配置文件指定的 `database-path`
2. `resources/geoip/GeoLite2-City.mmdb`（classpath）
3. `data/geoip/GeoLite2-City.mmdb`（默认位置）

#### 12.4.5 遗留问题与后续优化

1. **GeoIP数据库更新**：建议添加定时任务自动更新GeoIP数据库
2. **攻击链可视化**：建议使用图表库（如ECharts）增强时间线可视化效果
3. **导出功能**：建议添加溯源结果导出功能（CSV/JSON）
4. **IP关联分析**：建议添加IP之间的关联分析功能

#### 12.4.6 验收确认

- [x] 4.1.1 集成GeoIP库
- [x] 4.1.2 实现地理位置查询服务
- [x] 4.2.1 创建IP画像数据结构
- [x] 4.2.2 实现IP画像查询服务
- [x] 4.3.1 创建攻击链数据结构
- [x] 4.3.2 实现攻击链查询服务
- [x] 4.4.1 创建溯源查询Controller
- [x] 4.5.1 创建IP画像页面
- [x] 4.5.2 实现IP基本信息卡片
- [x] 4.5.3 实现IP历史统计卡片
- [x] 4.5.4 实现攻击链时间线组件
- [x] 4.5.5 实现攻击记录列表
- [x] 4.5.6 实现防御记录列表
- [x] 4.5.8 创建溯源查询页面
- [x] 4.5.9 实现溯源结果展示
- [x] 4.5.10 添加溯源入口集成
- [x] 4.6.1 功能测试
- [x] 4.6.2 前端测试

---

### 12.5 阶段五：优化配置同步（已完成）

**状态**：`已完成`  
**完成日期**：2026-03-26  
**实际工作量**：0.5天

#### 12.5.1 代码实现

##### 1. 配置同步数据结构

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/dto/ConfigSyncDTO.java`
- `monitor-service/src/main/java/com/network/monitor/dto/ConfigSyncStatusDTO.java`

**实现内容**：
- 配置同步DTO：包含版本号、同步时间、配置列表、同步类型
- 同步状态DTO：包含网关连接状态、同步统计、成功/失败次数

##### 2. 配置同步服务

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/service/ConfigSyncService.java`
- `monitor-service/src/main/java/com/network/monitor/service/impl/ConfigSyncServiceImpl.java`

**实现内容**：
- 获取同步状态：`getSyncStatus()`
- 同步所有配置：`syncAllToGateway()`
- 检查网关连接：`checkGatewayConnection()`
- 记录同步结果：`recordSyncSuccess()`, `recordSyncFailure()`
- 同步统计：成功次数、失败次数、最后同步时间

##### 3. 配置变更自动推送

**新增文件**：
- `monitor-service/src/main/java/com/network/monitor/event/GatewayConfigSyncListener.java`

**实现内容**：
- 监听配置变更事件（ConfigUpdateEvent）
- 自动识别网关配置项（以"gateway."开头）
- 异步推送配置到网关

##### 4. 配置同步API接口

**修改文件**：
- `monitor-service/src/main/java/com/network/monitor/controller/outer/GatewayConfigManageController.java`

**新增接口**：
- 获取同步状态：`GET /api/gateway/config/sync/status`
- 同步所有配置：`POST /api/gateway/config/sync/all`

##### 5. 前端同步状态显示

**修改文件**：
- `monitor-service/src/main/resources/static/js/sys-config.js`

**实现内容**：
- 增强网关状态检测：显示同步成功/失败次数
- 同步状态实时更新
- 同步操作结果反馈

#### 12.5.2 测试报告

##### 编译测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| monitor-service 编译 | ✅ 通过 | 无编译错误 |
| gateway-service 编译 | ✅ 通过 | 无编译错误 |
| 新增类依赖注入 | ✅ 通过 | 所有Service正确注入 |

##### 功能验证

| 验证项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| 配置同步状态查询 | 返回同步统计信息 | 正确返回状态 | ✅ 通过 |
| 配置变更自动推送 | 网关配置变更后自动推送 | 自动推送成功 | ✅ 通过 |
| 手动同步所有配置 | 推送所有网关配置 | 推送成功 | ✅ 通过 |
| 前端状态显示 | 显示同步成功/失败次数 | 显示正确 | ✅ 通过 |

#### 12.5.3 开发文档

##### 配置同步流程

```
配置变更 → 发布ConfigUpdateEvent → GatewayConfigSyncListener监听
    │
    └── 判断是否为网关配置（gateway.*）
        │
        ├── 是 → 异步推送到网关 → 记录同步结果
        └── 否 → 忽略
```

##### 同步状态数据结构

```json
{
    "gatewayConnected": true,
    "lastSyncTime": "2026-03-26T10:30:00",
    "lastSyncVersion": "1742976000000",
    "totalConfigCount": 15,
    "syncedConfigCount": 15,
    "syncSuccessCount": 10,
    "syncFailureCount": 2,
    "syncStatus": "NORMAL",
    "message": "同步成功，共15项配置"
}
```

##### 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `ConfigSyncDTO.java` | 新增 | 配置同步数据传输对象 |
| `ConfigSyncStatusDTO.java` | 新增 | 同步状态数据传输对象 |
| `ConfigSyncService.java` | 新增 | 配置同步服务接口 |
| `ConfigSyncServiceImpl.java` | 新增 | 配置同步服务实现 |
| `GatewayConfigSyncListener.java` | 新增 | 配置变更监听器 |
| `GatewayConfigManageController.java` | 修改 | 添加同步状态API |
| `sys-config.js` | 修改 | 增强同步状态显示 |

#### 12.5.4 已有功能确认

以下功能在之前的开发中已实现，本阶段进行了增强：

1. **网关配置缓存**（GatewayConfigCache.java）
   - 从监测服务拉取配置
   - 本地缓存管理
   - 配置更新接口

2. **配置同步控制器**（GatewayConfigSyncController.java）
   - 接收配置推送
   - 配置同步接口
   - 配置刷新接口

3. **定时同步任务**（ConfigSyncTask.java）
   - 每5分钟自动同步
   - 同步统计记录

4. **网关配置服务**（GatewayConfigService.java）
   - 配置读取和更新
   - 推送配置到网关

#### 12.5.5 遗留问题与后续优化

1. **配置版本回滚**：建议添加配置版本历史，支持回滚
2. **配置差异对比**：建议添加配置差异对比功能
3. **批量配置导入导出**：建议支持配置的批量导入导出

#### 12.5.6 验收确认

- [x] 5.1.1 设计配置同步数据结构
- [x] 5.1.2 创建网关配置接收接口（已有）
- [x] 5.2.1 实现配置变更监听
- [x] 5.2.2 实现配置推送服务（已有）
- [x] 5.2.3 网关配置缓存更新（已有）
- [x] 5.3.1 功能测试
- [x] 5.3.2 验证配置变更后网关同步更新
